# nohdol-study 하네스 변경 이력

| 날짜 | 변경 내용 | 대상 | 이유 |
|---|---|---|---|
| 2026-08-05 | 임베드 자산 경로를 노트 디렉터리에서만 찾던 것을 vault 상대로 해석하도록 수정 — `wiki/<주제>/` 노트가 `wiki/assets/`를 임베드하는 실제 배치를 인식 | `diagram/scripts/check.py`(신규 `embed_resolves`)·`check_test.py`, `diagram` SKILL "Rendering to SVG" | 사내 agent-eval 배선 문서를 노트화하다 19노드 flowchart를 D2로 승급하고 `![[assets/agent-eval/wiring.svg]]`로 임베드했더니 `PostToolUse` 훅이 "embedded asset not found"로 저장을 막았다. 파일은 `wiki/assets/agent-eval/wiring.svg`에 정상적으로 있었다. 원인은 검사기가 `path.parent / target`과 `path.parent / "assets" / name` 두 후보만 보는 것인데, **Obsidian 위키링크 임베드는 노트의 디렉터리가 아니라 vault 전체에서 해석된다.** 🔥 **오탐이 신규 노트만의 문제가 아니었다** — 실 vault를 훑으니 `AI Transformation 핵심 엔지니어링 위키`(`assets/ax-wiki/ecosystem.svg`)와 `NVIDIA A100 4장 기반 사내 AI LLM 통합 플랫폼 구축 가이드`(`assets/a100-ai-platform/architecture.svg`)도 **이미 같은 오탐 상태**였고, 두 노트 모두 Obsidian에서는 멀쩡히 렌더된다. 즉 검사기가 실제로 깨진 것을 잡은 적이 없는 조건이었다. 드리프트 시점은 2026-08-04로 보인다 — 그날 루트에 흩어져 있던 69개 노트를 주제 폴더 8개(깊이 1)로 옮기면서 노트와 `assets/`의 상대 위치가 바뀌었는데, 검사기의 경로 가정은 평면 배치 그대로 남았다. **위로 걷되 무한정 걷지 않는다**: `wiki/`를 만나면 멈추고, 그런 이름이 없는 배치(테스트 임시 디렉터리 등)에서는 3단계로 끊는다 — 상한이 없으면 상위 어딘가의 동명 파일이 **없는 자산을 있다고 통과시키는** 반대 방향 오탐이 된다. **검증**: 신규 테스트 2종(`wiki/AI Transformation/hub.md`가 `wiki/assets/ax-wiki/ecosystem.svg`를 임베드하면 통과, 같은 배치에서 실제로 없는 파일은 여전히 exit 1로 적발), 기존 테스트 전부 통과. 실 vault에서 오탐 2건 → 0건, 신규 노트 저장 성공. SKILL의 "소스를 노트 옆 `assets/`에 둔다"는 문구도 실제 관행(`wiki/assets/<그룹>/`)과 어긋나 있어 함께 정정했다 |
| 2026-08-03 | 실행 래퍼가 매 실행 추적본과 운영 사본의 SHA-256을 대조해 어긋나면 로그 맨 앞에 경고 | `examples/feed_scraper/run_scraper.sh`, 신규 `run_scraper_test.sh`, `examples/feed_scraper/README.md`, `docs/guides/feed-scraper.md` | 사용자가 "8-2 geeknews 위치 잘못 생성 됐어"로 신고했다. 원인은 코드가 아니라 **배포**였다 — launchd가 부르는 것은 비추적 `_workspace/feed_scraper/scrape.py`인데 이 사본이 2026-07-26자였고, 하루 전 커밋 `0b41a3c`(산출물을 `raw/`·`wiki/` 두 층으로 분리)가 복사되지 않았다. 구버전은 `VAULT_DIR = wiki/GeekNews` 하나만 알므로 08-03 06:00 실행이 08-02분을 통째로 `wiki/`에 만들었다 — 일일 리포트 1개, 월 인덱스 1개(`raw/`에 같은 이름이 이미 있어 `[[2026.8 인덱스]]` 위키링크가 모호해졌다), 그리고 폐기된 `주제/` 하위 폴더에 주제 문서 1개. **어제 고친 결함이 그대로 재현됐고 로그는 정상이라고 말하고 있었다.** 조용한 실패라는 점이 핵심이다 — 잘못된 자리에 파일이 생겼다는 것을 사람이 눈으로 보고서야 알았다. 이 구조의 대가가 "엔진이 두 곳에 있다"는 것인데 가이드는 설치 시 복사만 안내하고 **코드 변경 후 재복사 절차가 없었다**. 사용자가 고칠 방향을 지정했다("run_scraper.sh에서 해시 비교해서 경고하게 해줘"). 대조 대상은 실행 동작을 정하는 셋(`scrape.py`·`run_scraper.sh`·`requirements.txt`)만이다 — `README.md`와 `sources.local.example.toml`은 갈라져도 수집 결과가 달라지지 않는데 경고만 잦아지면 경고 자체를 읽지 않게 된다. **어긋나도 멈추지 않는다**: 낡은 코드로 도는 것이 수집이 끊기는 것보다 낫고, 사본 쪽을 먼저 고친 경우도 있으므로 복사 방향은 사람이 정하도록 양쪽 짧은 해시와 두 방향 안내를 함께 찍는다. 추적본이 보이지 않으면(사본만 떼어 다른 곳에 둔 배치) 검사를 건너뛴다 — 래퍼가 자기 위치에서 모든 경로를 유도한다는 기존 설계를 깨지 않기 위해서다. **검증**: 신규 `run_scraper_test.sh` 10종(동일하면 침묵·검사를 지나 venv 확인까지 도달·엔진 어긋남 지목·고치는 명령 동반·손대지 않은 파일 미보고·어긋나도 실행 계속·맞추면 해소·래퍼 자신의 어긋남·추적본 부재 시 침묵·추적본 자리에서 자기 비교 침묵) 통과, `SYNCED`를 비우는 변이에서 실패함을 확인해 테스트가 실제로 회귀를 잡는지 확인했다. 운영 사본에서도 실제 실행으로 정상 침묵과 어긋남 경고 양쪽을 확인했다. 함께 잘못 생긴 vault 파일 3종을 정리했다 — 일일 리포트를 `raw/geeknews/2026.8/`로 이동, 중복 월 인덱스 삭제 후 `refresh_month_index`로 2건 재생성, 주제 문서의 8-02 항목 1건(`gn:32043`)을 정본 `wiki/GeekNews/시스템과 데이터베이스.md`의 새 `## 2026-08` 섹션으로 병합(중복 마커 부재 확인) 후 `주제/` 폴더 제거 |
| 2026-08-03 | `study-video`의 프레임 추출을 기본 2-pass에서 **조건부 확인**으로 격하하고, 프레임을 vault에 저장하지 않도록 변경 | `study-video` SKILL(description·Why·절차 5·7·8·With/without), `.agents/skills/README.ko.md`, 루트 `README.md` | 비용 절감 축을 찾던 중 사용자가 "문서에 영상 프레임은 필요없다"고 지정했다. 실측 근거가 있었다 — 전날 노트화에서 프레임 4장을 뽑으려고 **65MB 영상 전체를 받았다.** `--detail transcript`만 주면 자막만 받고 영상은 안 받는데, `--timestamps`를 붙이는 순간 전체 다운로드가 딸려온다. 그런데 기존 절차는 pass 2를 **기본 단계로** 적어 두어 매 영상마다 이 비용이 붙었다. 🔥 **핵심은 프레임의 용도가 둘로 갈린다는 것이다** — 노트에 넣는 삽화와, 화자의 수치 주장을 확인하는 수단. 전날 프레임이 값을 한 건 후자였고(“6배”·“0.4%”가 축·단위 없는 개념 도식임을 확인), **그 확인 결과는 이미지가 아니라 문장으로 남는다.** "축·단위·측정 조건이 없다"가 검증표에 들어가면 이미지는 같은 사실을 여는 사람에게만 다시 전달할 뿐이다. 그래서 절차 7을 "vault에 복사"에서 "확인 결과를 검증표에 문장으로 쓰고 프레임은 작업 디렉터리와 함께 버린다"로 뒤집었다. 예외는 **주장 자체가 겉모습에 관한 것**일 때뿐이다(설명 중인 도식 O, 감사 대상 숫자 X). 절차 5에는 실행 전에 **화면이 답할 질문을 먼저 명명하도록** 요구를 넣었다 — 소득 없는 pass 2는 다운로드 비용만 남긴다. ⚠️ **`--download-sections`는 해법으로 쓰지 않는다고 명시했다** — 잘라낸 파일은 0초가 원본 T초라 원본 타임스탬프로 프레임을 뽑으면 **조용히 엉뚱한 지점**이 나온다. 절감 방법이 오답을 만드는 교환이라 채택하지 않았다. 전날 노트 2편에 이미 임베드된 프레임 2장은 검증표가 근거로 인용하고 있어 그대로 두었다 — 규칙 변경이 이미 선 근거를 소급해 끊지는 않는다. |
| 2026-08-02 | `hot.md` 예산을 2000→3000바이트로 상향하고, 흩어져 있던 "500토큰" 표기를 실제 게이트 값과 통일 | `vault-gardening/scripts/garden.py`(`DEFAULT_HOT_BUDGET`)·`garden_test.py`, `AGENTS.md`·`AGENTS.ko.md` 2절, `docs/specs/2026-07-25-phase1-study-harness.md`, `note-writer`·`using-study` SKILL | 하네스 영상 6종을 노트화한 뒤 `hot.md`를 갱신했는데 44%(3841→2167)를 줄이고도 예산을 넘어, 사용자가 "예산이 너무 타이트한 것 아니냐"고 물었다. **측정해 보니 짐작과 반대였다** — `hot.md`는 토큰당 3.30바이트, 영어 `AGENTS.md`는 4.00바이트다. 한글 음절은 3바이트인데 대략 1토큰이라 바이트도 토큰도 같이 늘고, **바이트 예산에서 한국어는 불리한 게 아니라 20% 유리하다.** 그래서 "한글이라서 타이트하다"는 상향 근거가 되지 못한다. 실제 근거는 따로 있었다 — **2000의 출처가 `docs/proposals/2026-07-25-nohdol-study-direction.md`의 "~500토큰"이고, 그건 vault가 30노트이던 때의 값이다.** 지금은 160노트·허브 7개로 캐시가 안내해야 할 주제가 5배가 됐는데 상수만 그대로였다. 실제 피해도 이미 있었다: 이 표 2026-07-25 항목의 규칙 4개 이관이 4002바이트 초과에 밀려 일어난 일이다. ⚠️ **단위는 바이트로 유지했다** — 토큰 근사로 바꾸면 토크나이저마다 달라 **아무도 재계산할 수 없는 숫자가 게이트에 들어간다.** 대신 상수 옆에 환산비(한글 3.3 / 영어 4.0)와 "왜 3000인가"를 주석으로 박고, 문서 5곳의 표기를 바이트 기준으로 맞춰 AGENTS.md와 게이트가 같은 숫자를 말하게 했다. 테스트는 4000바이트 초과 한 건만 보던 것을 **경계값(3000 통과 / 3001 적발)과 한글 경계(`가`×1000 통과 / ×1001 적발)**로 넓혔다 — 전자는 상수가 조용히 드리프트하는 것을, 후자는 게이트가 바이트 대신 문자를 세는 회귀를 잡는다. |
| 2026-08-02 | `subgraph` 제목의 단어를 노드로 세던 카운트 오탐 수정 | `diagram/scripts/check.py`(`count_nodes`)·`check_test.py` | 앞 항목을 마무리하며 남겨 둔 "`LLMOps 고성능 서빙 및 추론 최적화`가 노드 16개로 D2 승급선을 넘는다"를 처리하려고 다이어그램을 열어 보니 **실제 노드는 7개**였다(`USR`·`G_IN`·`G_OUT`·`RAG`·`VDB`·`VLLM`·`OBS`). 16 중 9개가 `subgraph Client Layer`·`subgraph Security & Guardrail Layer`처럼 **따옴표 없는 subgraph 제목의 단어**였다 — `Client`·`Layer`·`Security`·`Guardrail`·`Context`·`Serving`·`Engine`·`Observability`·`Evaluation`. `count_nodes`는 `style`·`classDef` 등만 건너뛰고 `subgraph` 라인은 그대로 훑었으며, `NOT_NODES`가 `subgraph`라는 단어 자체만 걸러 제목은 통과시켰다. 🔥 **오탐의 방향이 나빴다** — 검사기가 "이 다이어그램은 Mermaid를 넘었으니 D2로 렌더하라"고 **승급을 권고**하는 쪽으로 틀렸고, 그 말을 따랐다면 7노드 그림 하나를 SVG 파이프라인으로 옮기고 소스·렌더·임베드를 관리하게 됐을 것이다. 라벨을 따옴표로 감싸는 이 스킬의 규칙을 지킨 다이어그램은 애초에 이 오탐을 겪지 않으므로(제목이 라벨로 인식돼 `strip_labels`가 지운다) 규칙을 안 지킨 다이어그램만 골라 잘못된 조언을 받는 구조였다. `subgraph` 라인에서는 컨테이너 id 하나만 세도록 고쳤다 — 따옴표 있는 `subgraph SEC["…"]`는 `SEC`를, 따옴표 없는 `subgraph Client Layer`는 `Client`를 센다. **검증**: 새 테스트 2종(다섯 계층 7노드 다이어그램이 승급 권고를 받지 않음, id를 준 형태도 동일), 기존 테스트 전부 통과. 실 vault `wiki/` 전체 mermaid 23블록에서 **승급 권고 1건→0건**이 됐고 남은 권고는 없다. 대상 노트 자체도 스킬 규칙대로 subgraph 5개에 id와 따옴표 제목을 주고 노드 라벨을 따옴표로 감쌌다(`VDB[(…)]` 포함). vault 전체를 다시 훑어 따옴표 없는 subgraph 제목은 0건 |
| 2026-08-02 | Mermaid 도형으로 파싱되는 위키링크를 `diagram` 검사기가 잡도록 규칙 추가 | `diagram/scripts/check.py`·`check_test.py`·SKILL, 한글 스킬 안내 | 사용자가 노트 한 절을 붙여 놓고 "링크가 제대로 연결 안됐어"라고 했다. 대상 노트는 전부 존재했고 문제는 링크를 감싼 문법이었다. 백틱 코드 스팬 3건은 자명했지만 **정작 큰 쪽은 Mermaid `id[[텍스트]]` 8건**이었다 — 이건 **서브루틴 도형 문법**이고([Mermaid flowchart 문서](https://mermaid.js.org/syntax/flowchart.html)) 그 소스가 Obsidian 위키링크와 글자 단위로 같다. 그래서 붙여 넣은 링크가 오류 없이 파싱되어 노트 제목이 이중 테두리 상자로 그려지고, Obsidian은 펜스 안 링크를 해석하지 않으므로 **다이어그램은 vault에 없는 연결을 보여준다**. 🔥 **렌더 결과를 아무리 봐도 드러나지 않는다** — 상자 모양이 의도한 것과 정확히 같기 때문이고, 이것이 이 결함이 조용한 이유다. **검사기 넷이 각자 옳게 동작하면서 아무도 이것을 말하지 않았다**: 7/28에 `build_graph`가 코드 스팬·펜스 안 위키링크를 그래프에서 제외하도록 고쳤으므로(당시엔 오탐 제거가 목적) 죽은 링크는 `missing_targets`에 뜨지 않고, `garden`의 고아 판정은 프론트매터 `related`가 받쳐줘 통과하며, `diagram` 검사기는 문법적으로 유효한 도형이라 통과시켰다. **"의도된 링크가 죽었다"를 묻는 검사가 하나도 없었다.** 판정을 어떻게 결정 가능하게 만들지가 문제였는데 — 정당한 서브루틴 사용과 붙여넣은 링크는 소스가 같다 — vault 노트 목록을 조회하는 방식은 검사기를 배치에 결합시키므로 버리고, **따옴표로 갈랐다**: `[["텍스트"]]`면 의도한 도형, 맨 `[[텍스트]]`면 링크로 본다. 이 스킬은 이미 flowchart 라벨 전체에 따옴표를 요구하므로 새 부담이 아니고, 두 수리 경로(`["텍스트"]` 평범한 상자 + 본문 링크, `[["텍스트"]]` 도형 유지)가 메시지에 함께 나온다. 다만 검사기가 관용적이던 지점 하나를 조인 것은 맞다 — 기존 테스트가 `A[[Some Note]]`를 **통과시키도록** 고정해 두고 있어 함께 뒤집었다(그 테스트의 원래 의도인 "괄호 규칙 오탐 방지"는 따옴표판으로 유지). **검증**: 새 테스트 4종(원본 형태 차단·메시지 확인, 수리 두 경로 통과, 산문 위키링크 무영향), 기존 테스트 전부 통과, 수정 전 형태를 재현해 exit 1 확인. 실 vault `wiki/` 전체 mermaid 23블록에 돌려 신규 오탐 0건. **한계** — 따옴표 없는 서브루틴을 진짜로 의도한 경우도 걸린다. 이건 설계상 그렇고 따옴표를 붙이면 해소된다. **덧붙임**: 수리 결과를 그래프로 검증하다 이 문서와 한글 안내에 처음 적었던 조언("링크는 본문이나 `related`에 두라")이 틀렸음이 드러나 함께 고쳤다 — `build_graph.py`에는 `related`라는 문자열이 아예 없고 노트 본문만 읽으므로, `related`에만 있는 링크는 그래프에도 `vault-gardening`에도 Stop 훅 도달 검사에도 존재하지 않는다. Obsidian 프로퍼티로서는 의미가 있어도 이 저장소의 판정에는 잡히지 않는다는 뜻이라, 다이어그램에서 뺀 링크는 **본문**에 써야 한다 |
| 2026-08-02 | GeekNews 일일 캡처의 저장 층을 `wiki/`에서 `raw/`로 이동 — 스크래퍼가 그 자리에 쓰도록 | `examples/feed_scraper/scrape.py`·`scrape_test.py`, `docs/guides/feed-scraper.md` | vault 원자성 점검에서 나왔다. `wiki/` 305개 노트 중 **177개가 자동 수집 캡처**였고 `status: seed` 185개 중 177개가 거기서 나와, 원자 노트를 세는 지표가 전부 수집량에 휩쓸리고 있었다. 하루치에 서로 무관한 글이 여러 개 들어가는 캡처는 정의상 원자 노트가 아니고 `AGENTS.md` 2절이 이미 `raw/`를 불변 원본, `wiki/`를 큐레이션 원자 노트로 나누고 있으므로, 규칙을 바꾼 게 아니라 어긋나 있던 배치를 규칙에 맞췄다. **노트를 옮기는 것만으로는 되돌아온다** — 사용자가 지적한 대로 스크래퍼가 계속 `wiki/`에 쓰면 다음 실행에서 원상복구되므로 `DAILY_DIR`을 `raw/geeknews`로 분리했다. 사람이 지나는 입구인 월 인덱스와 사람이 고른 결과인 주제 문서 7종은 `wiki/`에 남겼다. `refresh_month_index`는 목록을 `raw/`에서 읽고 인덱스를 `wiki/`에 쓴다. **테스트가 실제 버그를 잡았다** — 예전에는 일일 리포트를 같은 자리에 쓰면서 `wiki/GeekNews/`가 저절로 생겼는데 층이 갈리자 인덱스 쓰기가 `FileNotFoundError`로 죽었다(새 설치에서만 터지는 종류라 현 vault에서는 안 보였을 결함). 인덱스 쓰기에 `makedirs`를 추가했다. Obsidian은 wikilink를 vault 전체에서 이름으로 풀고 `vault-gardening`도 같은 이유로 `raw/` 파일명을 읽으므로 인덱스의 `[[2026-08-01]]` 177개는 그대로 걸린다 — 이동 전후 **깨진 링크 0** 동일. **같은 날 사용자가 경계를 한 번 더 밀었다** — "geeknews 인덱스도 raw로 보내도 되지 않을까. 그리고 geeknews/주제에 있는 것은 geeknews 경로에 둬도 될 거 같은데." 맞는 지적이다. 첫 판단에서는 월 인덱스를 "사람이 캡처로 들어가는 입구"라며 `wiki/`에 남겼는데, **그 파일에는 큐레이션 판단이 하나도 없다** — 폴더를 훑어 만든 링크 목록일 뿐이라 캡처와 같은 자동 생성물이다. 기준을 "입구인가"에서 **"사람의 판단이 들어갔는가"** 로 바꾸자 인덱스는 `raw/`로 가고, 그러면 `wiki/GeekNews/`에 주제 문서만 남아 `주제/` 한 겹이 무의미해진다(한 종류만 있는 폴더에 하위 디렉터리를 파면 경로만 길어진다). `VAULT_DIR`을 없애고 `DAILY_DIR`(raw, 자동 생성물)과 `TOPIC_DIR`(wiki, 사람이 정한 분류) 둘로 정리했다. 회귀도 그 기준으로 다시 썼다 — "월 인덱스는 wiki에 남는다"를 **"월 인덱스도 raw에 있다"·"raw 산출물은 wiki를 건드리지 않는다"·"주제 문서는 하위 디렉터리를 두지 않는다"** 로 교체. 생성 템플릿의 안내문도 함께 고쳤다 — 주제 문서가 "날짜순 원본은 `GeekNews/<연월>/`에 있습니다"라고 적고 있었는데 이제 틀린 경로다(이미 생성된 7개 파일도 수정). 월 인덱스 안내문은 **과거 달이 갱신되지 않는다** — `refresh_month_index`는 현재 달만 호출되므로, 이미 생성된 7개를 직접 통일했다. 같은 김에 콜아웃 세 곳을 `> [!NOTE]\n> 본문`에서 `> [!NOTE] 본문` 한 줄로 바꿨다 — vault 관례가 표지와 첫 문장을 같은 줄에 두는 것이라, 그러지 않으면 `note-writer`의 `unwrap.py`가 스크래퍼 산출물을 매번 위반으로 잡고 고치면 다음 실행에서 되돌아가 mtime만 흔든다. **검증**: 스크래퍼 테스트 전 항목 통과, 층 분리 3종을 회귀로 고정(캡처는 `RAW_ROOT/geeknews` 아래, 월 인덱스와 주제 문서는 `WIKI_ROOT` 아래). `garden.py` 실측 305노트 → 128노트, 깨진 링크 0 유지 |
| 2026-08-01 | 루트 README의 텔레그램·스킬 지도 드리프트 정정 | `README.md`, 로컬 `REGISTRY.md` 상태 대조 | 읽기 전용으로 재활성화된 텔레그램 봇을 점검하는 과정에서 루트 기능 설명만 과거의 “노트를 직접 기록” 동작을 유지하고 있었고, 실제 17개 스킬 중 `vault-search`도 스킬 트리에서 빠져 있었다. 텔레그램 설명을 검색·조회·문답과 도구 게이트의 실제 경계로 맞추고, 과장된 “완벽한 보안 격리” 표현을 구체적인 차단 범위로 바꾸며, 스킬 트리에 `vault-search`를 추가했다. 머신별 봇 실행 상태와 `d2` 설치 상태는 추적하지 않고 로컬 `REGISTRY.md`에서만 갱신했다 |
| 2026-08-01 | 외부 노트북 런타임으로 나가는 vault 자료를 막는 `PreToolUse` 유출 게이트 신설 — 기존 훅을 넓히지 않고 별도 훅으로 | 신규 `.agents/hooks/study-egress-guard.py`·`study-egress-guard_test.py`, `.claude/settings.json`, `.codex/config.toml`, `.agents/hooks/hooks_test.sh`, `AGENTS.md`·`AGENTS.ko.md` 5절, `study-install` SKILL §6b, 신규 [ADR 005](adr/005-egress-guard-for-external-runtimes.md), docs MOC, 루트 README | 사용자 질문("쓸지 사용하지 않을지 구분은 잘 판단 가능한 상태지?")에서 나왔다. 답을 확인해 보니 **안전 경계는 문서에 있는데 집행이 하나도 없었다** — `AGENTS.md` 5절은 이미 "vault 내용은 별도 명시 승인 없이 그런 서버를 거치지 않는다"고 적고 있었고, 그날 Colab MCP 첫 실사용에서 그 문장을 지킨 것은 훅이 아니라 에이전트의 판단뿐이었다. 기존 `PreToolUse` 매처는 `Write|Edit|MultiEdit|NotebookEdit|Bash`여서 `mcp__colab-mcp__*`가 애초에 걸리지 않았다. **승인 프롬프트는 이 구멍을 메우지 못한다** — 프롬프트가 묻는 것은 "이 도구를 실행할까"이지 "이 페이로드에 사용자의 노트가 들어 있나"가 아니고, 셀은 아무도 한 줄씩 읽지 않는 코드 덩어리다. 대비 대상은 부주의한 붙여넣기가 아니라 **그럴듯한 한 걸음**이다: "노트의 기존 수치를 노트북에서 대조하자"며 노트 본문이 셀로 들어가는 것. **`study-tool-guard`를 확장하지 않았다.** 그 훅은 `STUDY_SURFACE`가 프롬프트 없는 표면을 표시할 때만 깨어나고 근거는 "대화형에는 프롬프트가 있다"인데, 그 근거는 쓰기가 **어디에 떨어지는가**에는 성립해도 페이로드에 **무엇이 들어 있는가**에는 성립하지 않는다. 같은 파일에 밀어 넣으면 기존 훅의 문서화된 전제가 조용히 넓어진다. 게다가 그 훅이 지키던 텔레그램 봇은 2026-07-31에 은퇴해 현재 휴면이라, 살아 있는 게이트를 휴면 게이트에 얹지 않았다. 막는 것은 **vault의 구조**(지식 루트 절대 경로·디렉터리 이름, `vault/wiki|raw|index.md|log.md|hot.md`, 노트 계약 지문인 `verification:`+`checked:` 동시 출현, 위키링크)이고 막지 않는 것은 **수치**다 — 공개 데이터셋 실측값이 vault에 적혀 있다는 이유로 공개 데이터 코드가 막히면 이 게이트는 실습을 불가능하게 만든다. 위키링크 검사가 `[[1, 2]]`를 무시하는 것도 같은 이유다(중첩 리스트 리터럴을 막으면 수치 연산이 통째로 걸린다). **한계를 명시해 둔다**: 알려진 통로 하나에 대한 표적 검사이지 일반 DLP가 아니며, 바꿔 쓰거나 인코딩한 내용은 통과한다. Codex의 `PreToolUse` 지원은 라이브 세션에서 확인하지 않았고 TOML 파싱까지만 검증했다 — 검증된 경로는 Claude Code다. **검증**: 테스트 15종 통과. 그날 실제로 Colab에서 돌린 셀(WM-811K 9개 클래스 개수를 대조 상수로 들고 있는 셀)이 통과하는 것을 회귀로 고정했고, 노트 본문·경로·위키링크·중첩 인자 은닉·Antigravity 방언은 차단으로 고정했다 |
| 2026-08-01 | 텔레그램 봇의 기본 활성 스킬을 `vault-search`로 지정 — 강제 사전 검색이 아니라 기본 스킬로 | `examples/telegram_bot/bot.py`·README, `docs/guides/mobile-telegram-bot.md` | 사용자 제안("사용자 입력이 들어오면 vault-search를 기본적으로 실행하게 하면 별로인가", 뒤이어 "vault에 있는 것 기반으로 질문을 할 거긴 해"). 방향은 옳다 — 이 표면의 입력은 거의 전부 볼트 조회이고, "답하기 전에 기존 노트를 찾는다"는 `using-study` 1번 규칙은 산문이라 건너뛰어진다(`study-note-check.py`를 훅으로 만든 것과 같은 논리). **다만 "매 입력마다 봇이 코드로 검색해 결과를 프롬프트에 주입"은 채택하지 않았다.** 임베딩 검색은 무엇을 넣든 결과를 돌려주기 때문이다 — 실측으로 `"고마워"`가 GeekNews 「뒤처져도 괜찮습니다, 고마워요!」를 **0.453**, `"아까 그거 다시 설명해줘"`가 「Retry-now 자율 루프 에이전트」를 **0.535**로 올렸다(진짜 관련 있을 때는 0.61~0.62). 뒤엣것이 특히 나쁜데 **답이 이미 대화 이력에 있는 후속 질문**에 무관한 노트가 끼어드는 형태다. 게다가 발췌가 프롬프트에 미리 들어와 있으면 **노트를 열기 전에 그것을 인용할 유혹**이 생기는데, `vault-search`의 계약은 결과가 포인터일 뿐이라는 것이고 이는 지식 그래프와 같은 규칙이다. 스킬로 두면 모델이 인사·후속 질문에서 건너뛴다. **비용은 걸림돌이 아니었다** — 로컬 임베딩이라 외부 호출이 없고 드리프트 없을 때 쿼리가 **약 1.0초**, 노트 5개 변경 직후 첫 쿼리가 **약 43.9초**(재임베딩 포함)인데 이 값은 자주 돌릴수록 작아진다. 구현에서 걸린 지점은 **"고른 적 없음"과 "직접 껐음"을 같은 상태로 두면 안 된다**는 것이다 — `reset`이 `pop`이면 다음 메시지에 기본값이 도로 켜져 해제 버튼이 아무 일도 안 한 것처럼 보인다. 빈 문자열을 명시적 off로 저장하고 `active_skill_for()`가 둘을 가른다. **검증**: 상태 기계 테스트 12종 통과(기본값·선택·해제, `bot_state.json` 저장/복원 왕복에서 해제 유지, 해제는 재시작 복원 알림에서 제외, 고른 스킬은 알림 대상) |
| 2026-08-01 | 승인 프롬프트 없는 표면에서 지식 루트를 **읽기 전용**으로 전환 — 텔레그램 봇은 검색·조회·문답만 한다 | `.agents/hooks/study-tool-guard.py`·`study-tool-guard_test.py`, `examples/telegram_bot/bot.py`·README, `AGENTS.md`·`AGENTS.ko.md` 5절, `docs/guides/mobile-telegram-bot.md`, `study-install` SKILL §5·§6, 루트 README, docs MOC, 한글 스킬 안내 | 사용자 요청("문서 작성하는 기능들은 다 제거하고 검색 스킬들만 이용해서 사용, 즉 질의응답만"). 이 봇은 2026-07-31에 은퇴했는데 이유가 정확히 쓰기였다 — `note-writer`를 우회해 프론트매터 계약이 깨진 노트를 vault에 직접 남겼다(`f2a3fae4` @39). 그때 대응은 훅에 계약 검사를 넣는 것이었지만 **형식이 맞는 프론트매터와 남길 값어치가 있는 노트는 같은 것이 아니다.** 계약은 기계로 볼 수 있고 실제로 잘 봤다. 볼 수 없는 것은 주장이 검증됐는지, 이미 있는 노트와 겹치지 않는지, `index`·`log`·`hot`이 함께 움직였는지이고 셋 다 판단이다. 승인 프롬프트가 없다는 것은 **판단할 사람이 없다**는 뜻이므로, 검사를 더 정교하게 하는 대신 그 표면에서 지식 루트를 닫았다. `writable_roots()`에서 `vault`를 빼자 `wiki/` 노트·`raw/` 캡처·항해 파일·삭제·셸 리다이렉션이 한 지점에서 전부 막혔고, 도달 불가가 된 프론트매터 검사 90여 줄은 삭제했다(계약 강제는 대화형 세션의 `PostToolUse` 훅 `study-note-check.py`에 그대로 있다). `_workspace/`와 임시 디렉터리는 열어 뒀다 — **읽기가 부작용이 없는 것은 아니어서** `vault-search`의 의미 인덱스가 거기 있고 노트가 바뀌면 스스로 갱신하는데, 이걸 닫으면 검색 자체가 멈춘다. 봇 쪽에서는 `/skill` 버튼을 조회 스킬만 남기고(`vault-search`·`study-session`·`vault-gardening`; `recall`·`paper-search` 제거 — **켤 수는 있는데 마지막 단계에서 막히는 모드가 가장 나쁘다**) 매 요청에 읽기 전용 표면임을 프롬프트로 알린다. 훅만으로 충분해 보이지만 훅은 모델이 **호출을 결정한 뒤에야** 거절할 수 있고, 폰에서 그 왕복은 몇 분짜리 `생각 중...`으로만 보인다. **검증**: 훅 테스트 44종 통과. 뒤집힌 기대값을 명시적으로 고정했다 — 계약을 완전히 만족하는 노트도 차단, `raw/` 캡처 차단, 심링크·상대 경로를 지나서도 차단, `delete_file`로 노트를 지우는 것도 차단, `_workspace` 쓰기와 vault **읽기**는 통과 |
| 2026-08-01 | MCP 서버 설치를 프로필 게이트로 명문화 — `corporate` 금지, `study-install`에 colab-mcp 선택 확인 절차 추가 | `AGENTS.md`·`AGENTS.ko.md` 5절, `study-install` SKILL §5, 한글 스킬 안내, `REGISTRY.md`(로컬 기록) | 사용자가 로컬 8GB 한계를 넘는 GPU 실습을 위해 공식 `googlecolab/colab-mcp`를 개인 머신에 등록한 직후 "사내에서는 MCP 설치 금지, study-install 시 설치 여부를 확인하라"를 요구했다. 이 서버는 에이전트가 실행하는 코드·데이터가 Google 런타임에서 도는 외부 전송이라 기존 "corporate는 선택적 제3자 전송 기본 금지" 조항의 적용 대상인데, MCP라는 형태가 조항에 명시돼 있지 않아 설치 시점마다 해석 여지가 남는 구조였다. 상시 정책은 AGENTS 5절에 한 줄로 명문화하고(금지는 항상 켜져 있어야 하므로 스킬에 두지 않는다), 반복 절차(감지 명령·`--scope user` 등록·OAuth·도구는 다음 세션 반영·결정의 REGISTRY 기록)는 study-install §5의 프로필 게이트 소절로 보냈다. corporate에서 `blocked-by-profile`을 기록하게 한 것은 이후 세션이 같은 질문을 다시 꺼내지 않게 하기 위함이다. 자동 테스트는 추가하지 않았다 — 게이트의 본체가 설치 인터뷰 행동이라 기계화 대상이 아니고, 감지는 문서에 적힌 한 줄짜리 결정적 명령이다 |
| 2026-08-01 | `unwrap.py`에 회귀 테스트 추가 | `note-writer/scripts/unwrap_test.py` | 이 저장소는 결정적 스크립트에 실행 가능한 테스트를 요구하는데 `unwrap.py`에는 없었다. `--write`로 노트를 제자리에서 고쳐 쓰는 스크립트라 위험한 실패는 "접힌 문단이 안 합쳐졌다"가 아니라 **줄바꿈이 구조였는데 합쳐진 것**이다 — 그렇게 망가진 노트는 겉보기에 멀쩡하다. 프론트매터·펜스 코드·틸데 펜스·표·제목·목록(글머리/번호)·구분선·들여쓰기 코드·각주 정의, 그리고 하드 브레이크 두 종(끝의 공백 두 칸, 역슬래시)과 목록 직전 산문 줄까지 "안 건드림"으로 고정했다. 보고 기본·`--write` 적용·2회차 무변화도 함께 고정. 테스트 작성 중 `unwrap()`이 `(텍스트, 건수)` 튜플을 돌려준다는 점이 드러나 테스트를 맞췄다(스크립트 동작은 정상) |
| 2026-08-01 | 로컬 임베딩 서버와 `vault-search` 스킬 신설, `study-install`에 설치기 추가 | 신규 `vault-search` SKILL·`scripts/semantic.py`·`semantic_test.py`, 신규 `study-install/scripts/install-embedding.sh`·SKILL, `note-writer`·`using-study`·`ingest` SKILL 배선, 루트 README·한글 스킬 안내 | 키워드 검색은 노트가 쓴 단어를 이미 짐작해야 찾힌다. 그 틈에서 이 vault의 반복 실패 두 가지가 나온다 — 같은 개념이 다른 말로 두 번 쓰이고, 기존 노트가 있는데도 모델 기억으로 답한다. **모델 선택이 이 도구의 성패를 갈랐다.** 정답 노트를 아는 한국어 질의 8건으로 실측하니 영어 중심 `nomic-embed-text`는 10위 안 적중 **2/8·MRR 0.067**로, "에이전트가 낸 결과를 배포 전에 자동으로 막는 방법"에 GeekNews의 "고객 이탈을 **막는**법"을 정답 노트보다 위에 올렸다(의미가 아니라 `막다`에 걸림). 같은 세트에서 다국어 `bge-m3`는 **8/8·MRR 0.573**. 그래서 기본은 다국어이고, 스크립트는 하드코딩 대신 서버에 설치된 모델을 물어 고른다(설치기가 사양에 맞춰 고르므로 고정 기본값은 안 받은 모델을 지목하게 된다). 인덱스가 있으면 그 모델이 우선한다 — 두 모델의 벡터는 비교 불가라 조용히 바꾸면 개선이 아니라 손상이다. **재빌드를 사람이 기억하는 구조를 만들지 않았다**: 트리거가 필요한 재빌드는 결국 안 돌고, 낡은 인덱스는 그럴듯한 결과를 주면서 방금 쓴 노트만 빼놓아 "vault에 없음"으로 읽히는 최악의 실패를 한다. `query`가 content hash로 바뀐 노트만 먼저 다시 임베딩한다(쓰기 세션 후 1~2초, 무변화 시 0). **밖으로 나가지 않는다** — vault에 이직 자료와 사적 노트가 있으므로 루프백이 아닌 엔드포인트는 설정이 아니라 스크립트가 거부하고, 인덱스는 동기화되는 지식 루트 밖 `_workspace/`에 둔다. 결과는 파생물이자 내비게이션이며 근거가 아니라는 규칙을 스킬·스크립트·출력 문구 세 곳에 명시했다(지식 그래프와 같은 규칙). **검증**: 테스트 22종(루프백 거부, 청킹이 제목을 함께 싣는지, 겹침, 정규화, 서버 없이 드리프트 판정, 인덱스가 vault 밖인지), 실 vault 286노트 3,866청크 색인 |
| 2026-08-01 | 임베딩 서버를 `brew services`가 아닌 전용 LaunchAgent로 띄우도록 확정 | `study-install/scripts/install-embedding.sh`, `REGISTRY.md`(설치처) | Homebrew의 ollama 서비스 plist가 `OLLAMA_FLASH_ATTENTION=1`·`OLLAMA_KV_CACHE_TYPE=q8_0`을 넣고, 이는 `--flash-attn on --cache-type-k q8_0 --cache-type-v q8_0`으로 llama-server에 전달된다. 둘 다 **생성형 모델의 KV 캐시**를 줄이는 옵션인데 임베딩 모델은 인코더라 그런 캐시가 없다. 결과는 조용한 반쪽 실패였다 — 서버는 `/api/version`에 답하는데 `ggml_metal_init` 전에서 멈춰 모델이 영원히 안 올라온다. 손으로 띄운 `ollama serve`는 `--flash-attn auto`에 cache-type 없이 같은 모델을 1초 안에 올렸다. **이 증상을 하드웨어 한계로 오진했고 그 판단으로 기능을 한 번 제거했다가 되돌렸다** — 근거로 삼은 `inference compute id=cpu` 한 줄은 30초 워치독이 만료된 별도 GPU 사전탐색의 산물이었고, 같은 로그 아래에 `MTL0 (Apple M1)`로 전 레이어가 GPU에 올라간 기록이 있었다. "여유 1.6GiB"도 같은 CPU 경로 수치이고 Metal 디바이스는 5460MiB를 보고했다. 그래서 설치기는 튜닝 변수를 하나도 설정하지 않는 자체 LaunchAgent를 쓰고, **포트가 열렸는지가 아니라 실제 임베딩이 돌아오는지로 검증한다** — 이 결함은 포트 검사를 전부 통과했다. Homebrew plist는 업그레이드 때 덮어써지므로 그쪽을 고치는 선택지는 애초에 없다 |
| 2026-07-31 | 노트 본문의 하드 줄바꿈 금지 규칙과 `unwrap.py` 검사 추가, vault 98개 파일 일괄 정리 | `note-writer` SKILL·`references/note-schema.md`·신규 `scripts/unwrap.py`, AGENTS·AGENTS.ko | 사용자 지적. 그동안 문단을 80열 근처에서 접어 써 왔는데, 마크다운에서 문단 내부 줄바꿈은 공백으로 렌더링되므로 **읽는 사람에게는 아무 차이가 없고 쓰는 쪽만 값을 치른다** — 단어 하나를 고치면 문단 전체가 다시 접히고, diff가 재접기 잡음으로 덮여 실제 변경이 보이지 않으며, Obsidian 편집기는 어차피 창 너비로 다시 접으므로 파일과 화면이 어긋난다. 규칙만 적으면 지켜지지 않으므로 판정을 스크립트로 옮겼다. 어려운 부분은 **어떤 줄바꿈이 구조인가**였다 — 표·코드블록·프론트매터·제목은 자명하지만 세 가지가 함정이었다: ① `코드:`/`결과:`/`데이터:` 처럼 짧은 라벨과 콜론으로 시작하는 줄은 문단처럼 보여도 실은 목록이라 합치면 안 된다. ② 줄 전체가 굵은 글씨면 사실상 소제목이므로 앞 줄이 이것을 삼키면 안 되는데, 첫 구현이 `**a**이므로 **b**` 같은 평범한 문단까지 소제목으로 오인해 187곳 중 일부를 놓쳤다 — 안쪽에 `**`가 없을 때만 소제목으로 보도록 고쳤다. ③ 목록 항목의 이어진 줄은 항목 안으로 들어가야 하는데 초판이 놓쳐 1,575곳만 처리했고, 보강 후 1,910곳이 됐다. **검증**: 적용 전 312개 마크다운 전체를 스크래치패드에 백업하고, 인용 부호(`>`)와 모든 공백을 제거한 문자열로 전후를 대조해 **내용 불일치 0건**, 표 행 수와 코드펜스 수 **변화 0건**을 확인했다. 초판 적용분은 백업에서 되돌린 뒤 개선판으로 한 번에 다시 적용했다 |
| 2026-07-31 | 대량 문서화용 `ingest` 배치 모드 신설 — 진행률을 체크박스가 아니라 노트의 인용에서 계산 | `ingest/scripts/queue.py`·`queue_test.py`·SKILL, 한글 스킬 안내 | AgentsView로 문서 작업 이력을 조사해 나온 패턴이다. `9d0d45ea`(사용자 턴 3개·출력 301k·peak 725k)와 `2324131b`(4개·467k·708k)는 `/goal` 한 줄로 압축 폴더 전체를 문서화한 세션인데, 둘 다 마지막 사용자 메시지가 뒷정리였다(`9d0d45ea` @278 "깨져있는 링크 제거해"). 원인은 **작업 목록이 대화 컨텍스트에만 있었다**는 것이다. 노트 수십 개째에 파일 목록이 시야에서 밀려나자 모델이 **출처 경로를 기억으로 쓰기 시작했고**, `source-files/`의 구분자를 `__`로 잘못 적은 인용 6건이 그렇게 들어왔다(실제 파일은 `_`, `slides-text/`·`notebooks/`는 진짜로 `__`라서 일괄 치환도 위험했다). 체크리스트 파일은 같은 약점을 물려받는다 — 체크하는 것도 기억해야 할 일이다. 그래서 **아무것도 저장하지 않는다**: 캡처 파일이 "완료"인지는 어떤 노트가 그 경로를 `sources:`에 실제로 적었는지로 계산한다. 세션이 끊겨도 노트에서 그대로 재개되고, 낡은 체크리스트가 거짓말할 수 없다. 대기 목록이 곧 **복사해 쓸 실재 경로**라서 기억으로 타이핑할 이유도 없앤다. 여기서 macOS 결함 하나가 드러났다 — 파일시스템은 한글을 NFD로 돌려주고 노트는 NFC로 저장돼, 경로를 문자열로 비교하면 `효율화`가 서로 다르다. 45개 캡처 전부를 "미인용"으로 보고했고(실제 6건 인용), `Path.exists()`는 OS가 가려주므로 문자열 비교를 하는 곳에서만 드러난다. `key()`로 양쪽을 NFC 정규화했다. **검증**: 테스트 13종, NFC 수정을 제자리에서 되돌리면 해당 테스트가 실패함을 확인. 실 vault에서 `source-files` 6/45, 강의 캡처 전체 54/101 |
| 2026-07-31 | Stop 훅의 도달 가능성 판정을 `garden.unreachable_notes` 한 벌로 통일 | `.agents/hooks/study-wrapup.sh`·신규 `study-note-record.py`·`hooks_test.sh`, `vault-gardening/scripts/garden.py` | 2026-07-26에 `garden.py`의 고아 판정을 "무엇이 나를 가리키는가"로 바로잡았는데 **Stop 훅은 그대로 뒀다**. 훅은 `grep -qF "$title" index.md`로 통과시켰고, index.md에 제목이 있는 이유는 두 가지다 — 주제 허브가 분류했거나(영속), 「최근 갱신」에 있거나(5줄 상한, 여섯 번째가 오면 밀려남). 즉 훅이 통과시킨 그 순간부터 밀려나기까지의 시간차만큼 조용히 고아가 되는 구조가 닷새 동안 남아 있었다. A100 노트가 정확히 그렇게 나갔다(`f2a3fae4` @395 "왜 진입점이 없어"). 진짜 결함은 **기록 여부와 도달 가능성을 한 검사로 뭉갠 것**이므로 두 질문을 갈랐다. 기록 검사는 index·log 어느 쪽이든 인정하는 기존 동작을 그대로 뒀다 — 이 오탐 프로필을 조이면 규칙상 옳아도 경보가 늘고, 이 훅은 이미 오탐으로 두 번 무시당한 전력이 있다. 도달 가능성은 `garden.unreachable_notes`를 호출한다(정의를 두 벌로 적으면 갈라진다는 것이 이 저장소의 반복 결함이다). **범위는 이번 세션에 쓴 노트로 한정한다** — vault 전체를 보면 기존 부채 하나가 매 턴을 막아 훅을 무시하도록 학습시킨다. 그래프는 뭔가 쓰였을 때만 만들고 286개 노트에 약 1초다. **검증**: 훅 테스트 통과, 새 회귀 테스트(index 평면 목록에만 있는 노트)는 수정 전 `study-wrapup.sh`에서 조용히 통과함을 확인 |
| 2026-07-31 | 노트를 저장하는 순간 검사하는 `PostToolUse` 훅 신설, 검사 정의를 `garden.py`에서 공유 | 신규 `.agents/hooks/study-note-check.py`·`study-note-check_test.py`, `vault-gardening/scripts/garden.py`(`note_contract_findings`·`source_anchor_findings` 분리), `.claude/settings.json`, `.codex/config.toml`, 루트 README | 검사기 자체는 좋았다 — Mermaid 라벨 검사는 Obsidian 번들 렌더러와 실제 라벨 235개를 대조해 오탐 0·미탐 0이었다. 없던 것은 **호출자**다. 모든 호출 지점이 SKILL.md 산문("마무리 전에 돌린다")이었고 산문 준수는 확률적이다. AgentsView 조사 결과 세 세션이 똑같이 시작한다 — `dbeebb2a` @0 "Error parsing Mermaid diagram! 가 발생하는 경우가 많네", `5773c188` @0 "unsupported markdown list 발생", `b43fb78d` @0 "줄바꿈에 걸친 인라인 서식 맹점". 셋 다 **이미 vault에 쓰이고 동기화된 뒤 사용자가 Obsidian에서 발견**했고, 원인 추적에 세션 하나씩 들었다(출력 토큰 합계 약 56만). 훅은 선택을 없앤다: 썼으니까 검사한다. 스킬을 아예 거치지 않는 작성자도 덮는데, 은퇴한 Telegram 봇이 프론트매터 계약을 깬 노트를 만든 이유가 정확히 스킬 우회였다(`f2a3fae4` @39). 범위는 **방금 쓴 파일 하나와 그 파일만으로 답할 수 있는 질문**으로 좁혔다 — 도달 가능성·백링크는 그래프가 필요하고, 클라우드 동기화 vault에서 편집마다 그래프를 만들면 글쓰기가 불가능해진다. 계약 검사와 출처 앵커 검사는 `garden.py`에서 함수로 꺼내 두 호출자가 같은 정의를 쓴다. **부수 성과**: 이 검사로 실 vault의 깨진 출처 앵커 19건을 정리했다 — 경로 오타 6건 수정, `_workspace/pt-experiments/` 인용 13건은 그 디렉터리가 git-ignored이고 지식 루트 밖이라 보존되지 않으므로 `raw/experiments/pt-2026-07/`로 스냅샷 후 21곳 갱신. `hot.md`도 3463→1975바이트로 정리하며 "지속가능경영보고서 PDF 미확보"라는 낡은 기술을 바로잡았다(이미 `raw/hynix-pt-2026/`에 있다). gardening 지적 23건 → 1건. **검증**: 새 테스트 20종 통과(오탐 방향도 고정 — 생성된 목록·vault 밖 파일·쓰기 아닌 도구·index.md 계약 면제), 기존 테스트 6종 전부 통과 |
| 2026-07-31 | Stop 훅에 "이번 세션이 건 링크가 해석되는가" 검사 추가, 그 전제로 `raw/` 캡처 링크 오탐 제거 | `.agents/hooks/study-note-record.py`·`hooks_test.sh`, `knowledge-graph/scripts/build_graph.py`·`build_graph_test.py`·SKILL, `vault-gardening/scripts/garden.py`·SKILL, `AGENTS.md`(8절)·`AGENTS.ko.md`, 한글 스킬 안내 | 사용자가 "서로 다른 세션에서 문서 작업을 시키면 충돌하지 않겠냐"고 물어 실측하던 중에 드러났다 — 묻는 그 시점에 **실제로 다른 세션이 돌고 있었고**, `P&T 28`을 쓰면서 **아직 존재하지 않는 `P&T 29`를 링크**하고 있었다. Stop 훅은 세 질문 중 둘만 하고 있었다: 노트가 기록됐는가, 무엇이 이 노트를 가리키는가. **이 노트가 가리키는 것이 존재하는가**는 아무도 묻지 않았다. 위키링크는 파일 **사이**의 참조라 파일 단위 검사로는 원리적으로 볼 수 없다 — 대상이 없어도 그 노트는 완전히 정상인 Markdown이라 저장되고 기록 검사도 통과하며, 결함은 다음에 누가 링크를 따라갈 때까지 조용히 남는다. 워크트리·Git 병합으로도 못 잡는 것이 같은 이유다(파일마다 well-formed하므로 conflict가 나지 않는다). **붙이려고 `missing_targets`를 열어 보니 3건 중 2건이 오탐이었다** — `build_graph`는 `wiki/`만 스캔하는데 Obsidian은 위키링크를 vault 전체에서 해석하므로, `raw/hynix-pt-2026/`에 **실재하는** 캡처 2개(`2026-07-31-미확보자료-5종-종결판정`·`2026-07-31-지속가능경영보고서2026-1차출처-발췌`)를 가리키는 정상 링크가 깨진 것으로 잡혔다. 그대로 훅에 걸었으면 **정상 노트로 매 턴 차단**했을 것이고, 그것은 이 훅이 이미 두 번 겪고 회복해야 했던 실패 모드(경고를 무시하도록 훈련시키는 것)다. 그래서 `--raw`를 먼저 넣었다 — **이름만** 읽고 노드도 엣지도 만들지 않으며 본문은 파싱하지 않는다. `garden.build_graph()`가 `raw/`가 있으면 자동으로 넘기므로 리포트와 훅이 같은 "깨진 링크" 정의를 쓴다(정의를 두 벌로 적으면 갈라진다는 것이 이 저장소의 반복 결함이다). 스코프는 앞의 두 검사와 같이 **이번 세션에 쓴 노트가 건 링크만** 본다 — 기존 깨진 링크까지 차단하면 남의 부채로 매 턴이 끝나지 않고 그것이 정확히 경고를 무시하게 만든다. 기존 부채는 `vault-gardening` 몫이다. **검증**: 실 vault 286노트·317링크에서 `missing_targets` 3건→1건, 남은 1건은 진짜(`P&T 22`→없는 `P&T 29`). 훅을 실행해 미등록 노트와 깨진 링크를 정확히 지목함을 확인. 테스트 추가 — 그래프 4종(raw 없으면 3건 보고, raw 주면 1건, 캡처는 노드·엣지가 되지 않음, raw 경로가 없어도 오류 아님), 훅 3종(등록·도달 가능한 노트의 깨진 링크는 차단하고 양끝을 지목, 링크를 고치면 해소, `raw/` 캡처 링크는 통과). **한계** — 훅은 발견의 첫 줄만 쓰므로 미등록·도달불가·깨진 링크가 겹치면 하나씩 순서대로 드러난다. 그리고 이 검사는 링크가 **해석되는지**만 보며 링크가 **적절한지**는 판단하지 않는다 |
| 2026-07-28 | 줄바꿈에 걸친 인라인 서식을 검사기 두 곳이 못 보던 맹점 수정 — 코드 스팬 안의 위키링크 오탐, `<br/>` 건너 백틱·링크 미탐 | `knowledge-graph/scripts/build_graph.py`·`build_graph_test.py`·SKILL, `diagram/scripts/check.py`·`check_test.py`·SKILL, `vault-gardening/scripts/garden.py`·`garden_test.py`·SKILL, 한글 스킬 안내 | 원인이 두 검사기에서 같다 — **markdown을 한 줄(혹은 한 구간)씩 읽는데, 인라인 서식은 줄에서 끝나지 않는다.** ① `build_graph`의 `INLINE_CODE`는 `` (`+)[^\n]*?\1 ``이라 한 줄 안의 짝만 지운다. CommonMark 코드 스팬은 같은 길이의 백틱을 **어디서든** 만나 닫히고 그 사이의 줄 끝은 공백이 되므로, `` `rg --files\n[[Ghost]]` ``는 통째로 코드다. 한 줄씩 읽으면 닫는 백틱을 영영 못 보고, 노트는 코드로 멀쩡히 렌더되는데 그래프만 "대상 없는 링크"를 보고한다(재현: `missing_targets`에 `Ghost` 1건). 줄 수를 보존하며 본문 전체에서 지우고, 빈 줄이 블록을 끝내므로 `(?!\n[ \t]*\n)`로 문단을 넘지 못하게 막았다 — 이게 없으면 산문에 홀로 떠 있는 백틱 둘이 그 사이 링크를 통째로 삼킨다(테스트로 고정). ② `diagram`의 라벨 검사는 `<br/>`로 자른 **구간마다** 7개 규칙을 돌린다. 목록·제목·인용·구분선은 줄 앞머리를 보는 블록 규칙이라 맞지만, **백틱 쌍과 `[텍스트](주소)`는 인라인**이고 Mermaid는 라벨 전체를 렉서 하나에 넘긴다. 로컬 `marked` 13.0.3으로 mermaid의 `<br/>`→줄바꿈 전처리를 적용해 직접 렉싱해 확인했다 — `` run `rg<br/>--files` here ``는 **codespan 토큰 하나**, `see [the doc<br/>page](http://x) now`는 **link 토큰**이 나온다. 즉 구간마다 보면 전부 평범한 글로 보이는 라벨이 실제로는 `Unsupported markdown`으로 치환된다. 규칙을 블록용·인라인용으로 나눠 인라인은 `<br/>`을 지운 라벨 전체에 돌린다. **굵게·기울임은 일부러 그대로 뒀다** — 같은 실험에서 `**굵게<br/>이어짐**`은 strong, `*기울임…*`은 em으로 나오고 둘 다 지원 타입이라 정상 렌더된다. 규칙이 아니라 측정이 경계를 정했다. 함께 드러난 같은 계열 결함 둘도 고쳤다 — `garden.py`의 index 링크 예산은 원문을 정규식으로 훑어 **펜스·코드 스팬을 전혀 가리지 않았고**(문법 예시를 담은 index로 재현하면 23개로 셈), 이제 그래프 빌더의 본문 읽기를 그대로 빌려 쓴다(두 벌로 적으면 정의가 갈라져 그래프에 없는 링크를 예산이 보고한다). `check.py`의 임베드 검사도 펜스 안을 그대로 세어 예시로 적은 `![[…svg]]`를 "없는 에셋"으로 지목했으므로, 줄 번호를 보존하는 마스킹을 거치게 했다. **검증**: 실 vault 231개 노트 0건·mermaid 25블록 정상(신규 오탐 0), 파이썬 테스트 11종 전부 통과, 새 테스트 3종은 수정 전 스크립트에서 각각 실패함을 stash로 확인(회귀 감지력 확인). 한계 — `marked`는 로컬에 있던 13.0.3이고 Obsidian 번들 버전을 직접 실행하지는 않았다. 코드 스팬이 줄 끝에서 닫히지 않는 것은 CommonMark 규정이라 버전과 무관하지만, 번들 오라클 대조는 하지 않았다 |
| 2026-07-26 | 도달 가능성 판정을 "가리키는 것이 있는가"로 바로잡고, 표 안 이스케이프 위키링크 파싱 결함 수정 | `vault-gardening` SKILL·`scripts/garden.py`·`garden_test.py`, `knowledge-graph/scripts/build_graph.py`·`build_graph_test.py`, 한글 스킬 안내 | 사용자가 A100 노트를 두고 "왜 진입점이 없냐"고 물어 확인해 보니 **인바운드 링크가 0**이었고, 그것을 잡아야 할 장치 넷이 모두 통과시키고 있었다. 원인은 하나로 모인다 — **"연결됨"의 정의가 아웃바운드 하나 또는 이름 한 번 언급으로 충족**되고, 정작 "누가 나를 가리키는가"는 아무도 보지 않았다. `build_graph`의 `orphans`는 `not links and not backlinks`라 링크를 여섯 개 내보내는 노트를 고아로 세지 않았고(`build_graph.py:472`), `garden.py`는 그 목록을 그대로 물려받았으며, Stop 훅은 `index.md`에 이름이 있으면 통과시키는데 그 이름이 있던 곳은 **다섯 건만 유지하고 밀어내는 「최근 갱신」**이었다. 즉 훅을 통과한 그 순간부터 밀려나기까지의 시간차만큼 조용히 고아가 되는 구조였다. `garden.py`의 판정을 `backlinks`가 없고 `categorized_under`도 없는 노트로 바꿨다 — 실측으로 `categorized_under`는 index의 주제 허브 5개만 잡고 「최근 갱신」은 세지 않음을 확인했으므로, 허브(인바운드 0이지만 index에서 진입)는 정당하게 제외되고 A100 같은 경우만 잡힌다. 함께 고친 것이 `normalized_target`의 `raw.split("|")`이다 — 표 안에서는 별칭 파이프를 `\|`로 이스케이프해야 하는데 그대로 split 해서 `vLLM과 PagedAttention\`가 남았고, **멀쩡한 링크 2개가 깨진 링크로 오탐되는 동시에 실제 엣지 2개가 누락**되고 있었다(오탐과 미탐이 같은 한 줄에서 나왔다). 실 vault 재측정 결과 깨진 링크 2건→0건, 도달 불가 0건. 테스트 5종 추가(아웃바운드만 있는 노트는 보고, 백링크만 있어도 제외, 분류만 있어도 제외, `\|` 링크가 엣지가 되고 missing에 안 오름) |
| 2026-07-26 | `d2`를 Phase 2 설치 대상에 편입 | `study-install` SKILL·`scripts/install-phase2-tools.sh`·`install-phase2-tools_test.sh` | `diagram` 스킬은 노드가 15개를 넘으면 D2로 올리라고 지시하는데, 정작 `d2`는 설치되지 않은 채 SKILL 문서에 "optional later diagram rendering"으로 이름만 올라 있었다. A100 플랫폼 노트(노드 30개)를 옮기려다 부재가 드러났고, 그 자리에서 설치를 물어봐야 했다 — 규칙이 escalate하라고 말하면서 escalate할 도구를 주지 않는 상태였다. `yt-dlp`·`ffmpeg`와 같은 단일 바이너리라 같은 package-manager 경로를 공유하므로 루프에 넣고 변수명을 `media_tool`에서 `brew_tool`로 넓혔다. `--check`도 d2를 보고한다 — 렌더러 부재가 다이어그램 빌드 실패 시점까지 숨지 않게 하려는 것이다. 테스트 2종 추가(설치 실패 시 d2를 개별로 지목, `--check` 출력에 d2 포함) |
| 2026-07-26 | 승인 프롬프트가 없는 표면에 PreToolUse 게이트 도입 — vault 밖 쓰기·홈 스윕·노트 계약 위반 차단 | 신규 `.agents/hooks/study-tool-guard.py`·`study-tool-guard_test.py`, `.agents/hooks/hooks_test.sh`, `.claude/settings.json`, `examples/telegram_bot/bot.py`·`bot_test.py`, `study-install` SKILL, `AGENTS.md`(5절)·`AGENTS.ko.md`, 텔레그램 가이드·README | 텔레그램 봇에 문서화를 시켰더니 macOS가 "python3.11이 미리 알림에 접근하려고 합니다"를 띄웠다. 원인은 두 겹이다 — ① 봇이 `--dangerously-skip-permissions`로 CLI를 띄우므로 **도구 호출을 막는 게이트가 하나도 없고**, ② 그 상태에서 모델이 `REGISTRY.md`가 이미 이름을 적어 둔 vault를 찾겠다고 `find ~ -maxdepth 3`을 돌려 `~/Library/Reminders` 같은 TCC 보호 폴더에 들어갔다. 다이얼로그가 CLI가 아니라 `python3.11`을 지목한 것은 TCC가 **책임 프로세스**(자식을 띄운 부모, 즉 봇)에 귀속시키기 때문이다. 같은 구멍으로 노트도 계약 없이 `wiki/`에 바로 쓰이고 있었다. 그래서 프롬프트가 없는 표면에서만 깨어나는 게이트를 만들었다 — 쓰기는 vault·`_workspace/`·임시 디렉터리로 한정하고(저장소 자체는 일부러 제외: 추적 파일 변경은 metaskill의 일이다), 홈 스윕과 보호 경로·개인 앱 AppleScript를 막고, `wiki/` 새 노트의 프론트매터 8개 필드·status·verification·날짜 형식·H1 일치를 검사해 어긋나면 note-writer로 돌려보낸다. **설계 도중 전제가 하나 틀린 것이 드러났다** — 봇이 부르는 `agy`는 Claude Code가 아니라 Antigravity CLI(1.1.7)여서 `.claude/settings.json` 훅이 애초에 적용되지 않았다. 실제 계약을 세션 관찰로 확보했다: 페이로드는 `toolCall.name`+PascalCase 인자(`write_to_file`은 `TargetFile`/`CodeContent`, `run_command`는 `CommandLine`/`Cwd`), 응답은 `{"decision":"deny"}`이며 Claude의 `block`은 모르는 값이다. 등록 위치도 달랐다 — 문서가 예시로 든 프로젝트 `.agents/hooks.json`은 **로드되지 않았고**(로그가 읽은 파일은 `~/.gemini/config/hooks.json` 하나뿐) 전역 파일에만 등록된다. 무출력이 양쪽 모두 "관여 안 함"이라는 것도 실측했다(무출력 훅을 건 채 쓰기가 그대로 통과). 라이브 4종으로 확인 — vault 밖 쓰기 차단, `find ~` 차단, 계약 위반 노트 차단(사유가 note-writer를 지목), 표시 없는 세션은 무간섭. 한계는 명시해 둔다: 셸은 패턴으로 완전히 가둘 수 없어(`python3 -c "open(...)"`) 실질 방어선은 쓰기 도구 경로 검사다. 테스트 49종 |
| 2026-07-26 | Mermaid 라벨의 markdown 렌더 실패와 `\n` 줄바꿈 무효를 검사 대상에 추가 | `diagram` SKILL·`scripts/check.py`·`check_test.py`, `note-writer` SKILL, 한글 스킬 안내, vault 노트 8개 | `파운데이션 모델…6대 발전 패러다임`의 다이어그램이 라벨 자리에 `Unsupported markdown: list`를 찍고 있었다. 작년 7월에 넣은 라벨 검사는 **파서**만 보므로 이걸 볼 수 없었다 — 이 다이어그램은 정상 파싱되고 실패는 렌더 단계에서 일어난다. 원인을 Obsidian 번들(`obsidian.asar`)의 mermaid 코드에서 직접 확인했다: 라벨은 따옴표 여부와 무관하게 `marked` 렉서로 넘어가고, 지원 토큰은 paragraph·text·strong·em·html·escape뿐이며 나머지는 `Unsupported markdown: <type>` 문자열로 치환된다. 즉 **따옴표는 파싱만 고치고 렌더는 못 고친다**. `1. 루프…`는 순서 목록이 되어 통째로 사라졌다. 함께 드러난 두 번째 결함이 `\n`이다 — markdown 라벨 경로에는 `\n`→줄바꿈 변환이 없어(변환은 비-markdown 경로인 `nonMarkdownToHTML`에만 있다) 역슬래시와 `n` 두 글자로 보인다. 검사기는 라벨 텍스트를 markdown 기호(순서/글머리 목록, 제목, 인용, 구분선, 백틱, 링크)로 판정하고 `\n`을 따로 잡으며, `<br/>` 뒤 구간도 검사한다(HTML 라벨 경로는 앞머리만, SVG 라벨 경로는 `<br/>`마다 블록이 시작되므로 둘 다 덮는다). **검증**: 번들 코드를 그대로 옮긴 오라클로 vault의 실제 라벨 235개를 렌더해 대조 — 깨진 라벨 40개(목록 26·`\n` 14)를 검사기가 정확히 40개로 잡아 오탐 0·미탐 0. 고친 뒤 235개 전부 정상. 번호는 `①`로 바꿨다 — `1)`도 목록 marker라 문장부호만 바꾸면 그대로 깨지고, `1\.` 이스케이프는 SVG 라벨 경로가 escape 토큰을 버려 번호가 사라진다. 영향 노트 8개(6개는 목록, 2개는 `\n`, `파운데이션…`은 둘 다) |
| 2026-07-26 | 소스 선택 기준을 건수에서 개념 밀도로 옮기고 ROS Discourse를 기본 꺼짐으로 | `examples/feed_scraper/scrape.py`, vault 노트·수집 문서 | 수집 61건을 실제로 훑은 첫 표본에서 **건수와 밀도가 반대로 움직였다.** ROS Discourse가 절반(30건)을 차지하면서 개념 노트감이 0건이었고 — 구인·밋업·안부·개별 프로젝트가 섞인다 — 반면 제목 필터를 거쳐 3건만 받은 Hugging Face는 3건 모두 값있었다. 그래서 ROS Discourse를 `enabled`에서 빼고 수집 문서도 지웠다. **카탈로그에서 지우지는 않았다**: ROS를 직접 쓰기 시작하면 바로 그 실무 스레드가 가장 값있어지므로, 근거를 주석에 남겨 되살릴 수 있게 뒀다. 함께 HF 필터의 재현율을 검증했다 — 로봇 도메인 낱말 30개(`vla`,`grasp`,`mujoco`,`isaac`,`locomotion`,`tactile`,`urdf`,`slam` 등)로 831건을 재대조해 **현재 9개 낱말이 놓친 로봇 글이 0건**임을 확인했고, 후보에 걸린 12건은 전부 LLM 글(`rl ` 8건, `reinforcement learning` 3건, `depth` 1건)이었다. 넓히면 재현율이 아니라 오염만 늘어난다는 뜻이라 그대로 뒀다. 두 실측 모두 표본 하나라는 한계를 노트 검증 표에 명시했다 |
| 2026-07-26 | Stop 훅의 자동 생성물 제외를 태그에서 노트 `type`으로 넓힘 | `.agents/hooks/study-wrapup.sh`·`hooks_test.sh` | 같은 날 넣은 월 인덱스 재생성이 곧바로 훅에 걸렸다 — `2026.7 인덱스`를 "index.md에 기록되지 않은 노트"로 차단했는데, 그것은 스크래퍼가 매 실행 다시 만드는 파생 목록이라 진입점이 항목별로 이름을 부를 대상이 아니다. 제외 규칙이 `feed`·`daily-scrap` 태그만 보고 있어 월 인덱스(`GeekNews`,`Archive`)와 주제 문서(`geeknews`,`curation`)가 빠져 있었다. 실제 분포를 보니 경계가 `type`에 이미 있었다 — 자동 생성물은 `index`(월 아카이브)·`moc`(주제·소스별 대기열)이고 손으로 쓴 지도는 `topic`·`source`다. 그래서 `index`와 `moc`을 제외하고 손으로 쓴 지도는 계속 검사한다. 날짜별 수집분은 `article`이므로 기존 태그 규칙이 그대로 잡는다. 테스트 3종 추가(index 제외, moc 제외, 손으로 쓴 허브는 미등록 시 차단·이름 지목) |
| 2026-07-26 | 수집한 날짜 파일이 아카이브 인덱스에서 빠져 고아가 되던 문제 차단 | `examples/feed_scraper/scrape.py`·`scrape_test.py` | vault-gardening이 `2026-07-25`를 고아 노트로 보고했는데, frontmatter의 `related`는 정상이었다. 그래프를 열어 보니 인바운드 0·아웃바운드 0이었다 — **`build_graph`는 frontmatter `related`를 엣지로 세지 않고 본문 위키링크만 센다.** 다른 날짜는 월 인덱스가 링크해 주는데 07-25만 인덱스에 없었다. 6개월치를 대조한 결과 누락은 그 하루뿐이었고, 과거 달은 일괄 정리 때 인덱스가 함께 만들어져 온전했다. 즉 **스크래퍼가 날짜 파일만 쓰고 인덱스를 손대지 않아 매일 새 고아가 생기는 구조**였다. `refresh_month_index()`가 그 달 폴더를 스캔해 목록을 다시 만든다 — 한 줄 덧붙이기가 아니라 재생성인 이유는 인덱스가 파일에서 재생산되는 파생물이고, 그래야 과거에 빠진 것도 함께 메워지기 때문이다. 안내문의 개수도 실제와 맞춘다. 내용이 같으면 쓰지 않아 실행마다 mtime을 건드리지 않는다(테스트 6종으로 고정) |
| 2026-07-26 | 지식 로그를 최신순으로 바꾸고 관련 규칙 문구를 함께 정정 | `AGENTS.md`(2·3·5절), `AGENTS.ko.md`, `note-writer` SKILL, `using-study` SKILL, vault `log.md` | 로그가 오래된 것부터 쌓여 최근 작업을 보려면 27건을 지나쳐야 했다. 세션 시작 맥락을 잡을 때 가장 자주 보는 것이 최근 기록인데 읽는 방향이 반대였다. 데이터 행만 역순으로 뒤집었고(정렬 후 비교로 내용 손실·변경 0 확인) 새 항목은 표 첫 줄에 넣는다. **"append-only"라는 표현이 규칙 네 곳에 있어 함께 고쳤다** — 그대로 두면 새 계약과 모순되고, 다음 세션이 문서를 따라 맨 아래에 붙이게 된다. 다만 바뀐 것은 읽는 방향뿐이고 "이미 적은 항목을 고치거나 지우지 않는다"는 계약은 그대로라, 그 점을 규칙과 `log.md` 머리말 양쪽에 명시했다. Stop 훅은 `log.md`의 날짜를 `sort`로 훑어 최대값을 찾으므로 순서와 무관하게 동작한다(재검증 완료) |
| 2026-07-26 | 텔레그램 봇 로그에 남던 API 토큰 차단 | `examples/telegram_bot/bot.py`, 신규 `examples/telegram_bot/bot_test.py` | 텔레그램 API는 토큰을 URL 경로에 담고(`/bot<TOKEN>/getUpdates`) httpx는 요청 URL을 그대로 INFO로 남긴다. 폴링이 10초마다 돌므로 로그가 토큰 사본으로 채워지고 있었다 — 실측에서 기존 `bot.log`의 **5,783줄**에 토큰이 있었다. `.gitignore`에 있어 커밋된 적은 없지만, 문제 상황에서 로그를 붙여넣거나 공유하는 순간 함께 나간다. 둘로 막았다 — httpx 로거를 WARNING으로 올려 정상 요청 로그를 없애고, 예외 메시지나 다른 라이브러리로 URL이 다시 새어 나올 경우를 위해 루트 **핸들러**에 마스킹 필터를 붙였다(로거가 아니라 핸들러인 이유는, 로거에 붙이면 그 로거를 거치는 기록만 걸러지기 때문이다). 기존 로그 5,783줄은 정규식 치환으로 마스킹했다 — 토큰 값을 몰라도 되도록 `bot<숫자>:<영숫자>` 패턴을 썼다. 재시작 후 42초(폴링 4회분) 동안 httpx 기록 0건을 확인했다. 테스트 9종 추가: httpx 레벨, 메시지·포맷 인자 마스킹, 비문자열 인자 보존, 핸들러 통과 출력, 루트 탐색 3종 |
| 2026-07-26 | 추적 파일에 남아 있던 설치 경로 하드코딩 제거 | `examples/telegram_bot/bot.py`, `docs/guides/mobile-telegram-bot.md` | `AGENTS.md` §1은 설치 경로를 추적 파일에 두지 말라고 하는데, 텔레그램 봇이 `STUDY_ROOT` 기본값으로 `/Users/nohdol/nohdol-study`를 담고 있었고 가이드 다이어그램에도 같은 경로가 있었다. 저장소를 복제한 다른 컴퓨터에서는 환경 변수를 반드시 줘야 돌아가는 상태였다. 피드 스크래퍼에 쓴 것과 같은 방식으로 바꿨다 — 위로 올라가며 `vault` 심링크를 가진 디렉터리를 찾으므로 `examples/`에서든 `_workspace/`에서든 같은 깊이라 그대로 동작한다(세 위치에서 확인). 루트를 못 찾으면 조용히 빈 `cwd`로 CLI를 띄우는 대신 시작 시점에 멈추고 이유를 말하게 했다 — 그 실패는 "봇이 답을 안 한다"는 증상으로만 보여 원인을 찾기 어렵다. 돌고 있는 봇은 `_workspace/` 사본으로 실행되므로 이 변경에 영향받지 않는다 |
| 2026-07-26 | 피드 스크래퍼에 제목 필터 추가, 로봇 소스를 6종으로 확대, 링크 텍스트의 날것 HTML 태그 차단 | `examples/feed_scraper/scrape.py`·README·`sources.local.example.toml`, `docs/guides/feed-scraper.md` | 셋을 함께 고쳤다. ① **깨진 렌더링** — ROS Discourse의 `What should go in the <license> tag?`가 그대로 저장돼 렌더러가 HTML 시작 태그로 읽었다. `defuse_html()`은 본문 전용이라 `feed` 제목에는 적용되지 않고 있었고, 링크 텍스트 안에서는 백틱이 중첩되므로 `clean_title()`이 `<`를 엔티티로 되돌리도록 했다(`&lt;license>`는 화면에 그대로 보인다). `<script>` 계열도 함께 무력화된다. ② **커버리지 공백** — 기존 셋(IEEE·The Robot Report·ROS Discourse)은 편집 소식·산업·ROS 실무를 덮지만 연구와 모델 축이 비어 있었다. 실측으로 Robohub(일 0.4), NVIDIA Robotics(일 0.2), Hugging Face(로봇)를 더해 일 0.6건 남짓만 늘리고 축을 채웠다. arXiv cs.RO는 API가 작동함을 확인했으나 일 34.9편이라 대기열이 아닌 질의 대상이므로 `paper-search`에 남겼고, Figure·Unitree·Boston Dynamics는 세 곳 모두 RSS 404라 구조적으로 불가함을 기록했다. ③ **`title_filter` 옵션** — HF 블로그는 831건 중 로봇 관련이 23건(2.8%)뿐이라 그대로 넣으면 LLM·디퓨전 글이 목록을 덮는다. 제목만 검사하는 필터를 넣어 실행에서 19건 제외·3건 수집을 확인했다. 본문을 보지 않는 이유는 GeekNews 관심어와 같다 — 주제가 스쳐 지나간 글이 대거 걸리고 정작 찾던 글은 제목에 그 말이 있다 |
| 2026-07-26 | Stop 훅의 마무리 판정을 수정 시각에서 파일 내용으로 전환 | `.agents/hooks/study-wrapup.sh`, `.agents/hooks/hooks_test.sh` | mtime만 비교하는 판정이 오탐 둘을 만들어 알림을 무시하도록 학습시키고 있었다. ① 노트를 index 갱신보다 몇 초 늦게 저장하면 이미 등록된 노트인데도 걸렸다 — 이 세션에서 실제로 34초 차이로 두 번 발동했고, vault가 Google Drive라 sync 클라이언트가 mtime을 다시 쓰기까지 해 같은 파일이 검사 때마다 나타났다 사라졌다 했다(AGENTS.md §5가 경고하는 상황). ② 피드 스크래퍼가 매일 아침 수집 문서를 다시 쓰므로 자동 실행이 있는 날은 반드시 걸렸다. 판정을 셋으로 나눴다 — 노트는 **제목이 `index.md`나 `log.md`에 있는지**로 보고(mtime은 검사 범위를 최근으로 좁히는 용도로만 남김), frontmatter `tags`에 `feed`·`daily-scrap`이 있는 자동 수집 문서는 지식이 아니므로 제외하며, 노트 목록을 담지 않는 `hot.md`는 **자신의 `updated:`가 `log.md`의 최신 날짜보다 이른지**로 판정한다. 셋 다 파일에 적힌 내용을 읽으므로 sync가 타임스탬프를 바꿔도 흔들리지 않는다. 차단 메시지도 어느 노트가 누락됐는지, 어느 날짜만큼 뒤처졌는지 지목하도록 바꿨다. 테스트 7종 추가(등록된 노트 통과, log 기재만으로도 통과, 미등록 노트 차단과 이름 지목, feed 문서 제외, hot 날짜 뒤처짐 차단, 날짜만 갱신해도 해소, 기록 파일 부재 구분) |
| 2026-07-26 | GeekNews 전용 스크래퍼를 다중 소스 피드 스크래퍼로 일반화하고 레퍼런스를 추적 영역으로 옮김 | 신규 `examples/feed_scraper/`, `docs/guides/feed-scraper.md`, `study-install` SKILL, 루트 README, 한글 스킬 안내, `docs/README.md`, `.gitignore`, `~/Library/LaunchAgents/com.user.study.feedscraper.plist` | 운영 중인 스크래퍼가 비추적 `_workspace/geeknews_scraper/`에만 있어 세 가지가 깨져 있었다. ① 저장소를 복제한 다른 컴퓨터에는 코드가 없다. ② `vault/scripts/`에 남은 구버전(158줄)이 운영본(653줄)과 공존해, 이번 세션에서 `log.md`가 기록한 `clean_title()`·`defuse_html()`을 찾다 구버전을 먼저 열었다 — 어느 파일이 도는지 알 수 없는 상태였다. ③ vault 절대경로와 소스 목록이 코드에 박혀 있어 다른 기계로 옮기려면 코드를 고쳐야 했고, 그 수정은 개인 선택을 추적 파일에 섞는다. 그래서 **엔진과 카탈로그는 추적**(`examples/feed_scraper/scrape.py`), **어느 소스를 켤지는 비추적**(`sources.local.toml`)으로 나눴다. vault 경로는 위로 올라가며 `vault` 심링크를 찾아 유도하므로 코드에 설치 경로가 없다. 파이프라인은 둘 — `feed`는 제목·링크만 쌓아 API 호출이 0이고(소스를 늘려도 외부 한도와 무관), `geeknews`는 기존 점수 게이트·요약·분류를 그대로 유지한다. 카탈로그에 IEEE Spectrum Robotics·The Robot Report·ROS Discourse를 실측값과 함께 추가했고, launchd 작업은 `com.user.geeknews.scraper`에서 `com.user.study.feedscraper`로 교체했다. 실행 검증: 4소스 동시 실행에서 GeekNews 정상·IEEE 중복 0·The Robot Report 15건·ROS Discourse 30건 |
| 2026-07-26 | 텔레그램 봇에 진행 표시·취소·타임아웃·상태 영속화 추가 | `examples/telegram_bot/bot.py`, `docs/guides/mobile-telegram-bot.md`, `.gitignore` | 로그상 21회 실행에 크래시는 0건이었으나 모바일에서 갇히는 경로가 셋 있었다. ① 진행 메시지가 정적이라 몇 분짜리 작업이 죽었는지 도는지 구분되지 않았다 — 5초마다 경과 시간으로 편집하고(`editMessageText`는 무음이라 알림이 발생하지 않음) 타이핑 표시를 4초마다 갱신한다. ② `process.communicate()`에 타임아웃이 없고 취소 수단도 없어 CLI가 멈추면 폰에서 손쓸 수 없었다 — `STUDY_RUN_TIMEOUT`(기본 1200초)과 `[⏹ 취소]` 버튼·`/cancel`을 넣고 부분 출력을 함께 반환한다. ③ 모델·강도·스킬이 메모리에만 있어 launchd `KeepAlive` 재시작 때 조용히 사라졌고, 사용자는 `study-session`이 해제된 줄 모른 채 문답을 계속했다 — `bot_state.json`으로 영속화하고 복원 사실을 알린다(대화 이력 자체는 CLI의 `--continue`로 이미 보존되고 있었으므로 유실되던 것은 스킬 접두사 주입뿐이었다). 동시 실행은 사용자 판단에 따라 차단하지 않고 세션 이력이 섞일 수 있다는 안내만 넣었다. 상태 파일은 chat ID를 담으므로 `.gitignore`에 추가 |
| 2026-07-26 | 모바일 가이드에서 영상 길이를 위험 요인으로 적은 서술 정정 | `docs/guides/mobile-telegram-bot.md` | "링크 하나가 긴 분석을 트리거한다"고 적어 재생 시간을 비용 위험처럼 제시했으나, 2-pass 설계상 비용은 길이가 아니라 트랜스크립트 분량과 선택한 타임스탬프 수에 비례한다. 이 저장소에서 102분 강의를 실측한 값(자막만 2.3MB, 트랜스크립트 27k 토큰, 프레임 12장 248KB)을 근거로 넣고, 실제 제약 세 가지(자막 없는 영상의 Whisper 폴백, 모바일 대기 시간, 의도 없이 붙여넣는 링크)로 교체했다. 존재하지 않는 위험을 경고하면 진짜 위험이 묻힌다 |
| 2026-07-26 | 모바일 가이드의 `study-video` 설명 정정 | `docs/guides/mobile-telegram-bot.md` | 직전 커밋에서 `study-video`를 "명시적 요청 전용"으로 적어 사용자가 스킬 이름이나 "이 영상 공부해줘"를 말해야 하는 것처럼 오해를 만들었다. 스킬 description의 `explicitly selected`가 한정하는 것은 스킬이 아니라 **영상**이며, URL을 붙여넣으면 다른 스킬과 동일하게 자동 라우팅된다(이 저장소 세션에서 실제로 그렇게 동작). Safety gate가 실제로 강제하는 것은 `--no-whisper` 상시 적용과 타임스탬프 선별 프레임이므로 그 둘로 바꿔 적고, 모바일에서 링크 하나가 긴 분석을 트리거할 수 있다는 경고를 추가했다 |
| 2026-07-25 | 텔레그램 봇 `/skill` 메뉴를 명시 전환이 필요한 4종으로 축소하고 `/understand` 명령 제거 | `examples/telegram_bot/bot.py`·README, `docs/guides/mobile-telegram-bot.md` | 메뉴가 스킬 라우팅 계층을 반영하지 못하고 있었다 — `note-writer`·`knowledge-graph`는 요청 문구로 자동 선택되는데 버튼으로 고정하면 사용자가 라우터보다 먼저 스킬을 못박아 오히려 잘못된 스킬에 갇힌다. 대화를 그 모드로 유지하고 싶은 것만 남겨 `study-session`·`recall`·`paper-search`·`vault-gardening` 4종으로 재구성하고, 별도 명령이던 `/understand`(5개 모드 버튼)는 `understand` 스킬이 아홉 모드를 내부 라우팅하므로 중복이라 제거했다. 상호 배타 상태 관리(`user_current_understand`), 콜백 분기, `/status` 표시, 프롬프트 주입 경로까지 함께 삭제. 명시 전용인 `study-video`·`/watch`를 버튼으로 노출하지 않는 이유도 가이드에 명문화했다 |
| 2026-07-25 | 이미지 임베드를 지식 그래프 엣지로 세지 않도록 수정 | `knowledge-graph/scripts/build_graph.py`·`build_graph_test.py` | `![[assets/....jpg]]` 임베드가 일반 위키링크로 집계되어, 프레임을 싣는 노트마다 "대상 없는 링크"가 보고되고 있었다 — 파일은 바로 옆에 있는데도. 실제 vault에서 오탐 16건이 리포트 최상단을 채웠고, 이는 가장 중요한 섹션을 무시하도록 학습시킨다. `normalized_target()`에서 media 확장자를 빈 문자열로 반환해 두 호출 지점(본문 링크·index 분류) 모두에서 제외했다. 노트 트랜스클루전(`![[다른 노트]]`)은 확장자가 없으므로 엣지로 남는다 |
| 2026-07-25 | hot.md에 쌓이던 하네스 규칙을 각 스킬로 이관 | `note-writer/references/note-schema.md`, `study-video` SKILL | `hot.md`는 AGENTS.md가 "캐시이지 authority가 아니다"로 정의한 파일인데 하네스 규칙 4개가 거기에만 있었고, 500토큰 예산에 눌려 잘려나가면 규칙도 함께 사라지는 구조였다(실제로 4002바이트로 예산 초과). 제목의 슬래시 금지 규칙은 노트 스키마로, YouTube 자동 자막의 롤링 중복·고유명사 오전사 처리 규칙은 `study-video` 절차로 옮겼다. 나머지 둘(Mermaid 라벨, index 정책)은 이미 각 스킬에 있으므로 캐시에서 삭제했다 |
| 2026-07-25 | index를 진입점으로 유지하는 정책 신설과 비대화 자동 검출 | `note-writer` SKILL·신규 `references/index-policy.md`, `vault-gardening` SKILL·`scripts/garden.py`·`garden_test.py`, 루트 README, 한글 스킬 안내 | `note-writer`의 index 지침이 "navigational entry"뿐이라 원자 노트를 전부 나열해도 규칙 위반이 아니었고, 실제로 노트 24개에 index가 59줄까지 자랐다 — 진입점의 크기가 주제 수가 아니라 노트 수를 따라가면 200개 규모에서 아무도 읽지 않는 벽이 된다. 주제당 허브 노트 한 줄, 원자 노트 나열 금지, 최근 갱신 5줄 상한(정본은 `log.md`)을 참조 문서로 명문화하고, `garden.py`에 `--index-link-budget`(기본 15) 검사를 추가해 임베드는 제외하고 서로 다른 위키링크 대상 수로 판정하도록 했다. 처방은 항상 허브 신설이며 항목 삭제로 숫자를 맞추는 것이 아님을 스킬 양쪽에 명시했다 |
| 2026-07-25 | Mermaid 라벨 문법 검사 추가와 note-writer 다이어그램 게이트 연결 | `diagram` SKILL·`scripts/check.py`·`check_test.py`, `note-writer` SKILL, 한글 스킬 안내, vault 노트 9개 | vault의 Mermaid 블록 26개 중 9개(35%)가 실제 Mermaid 파서에서 파싱 실패하고 있었고 기존 검사기는 전부 통과시켰다 — 괄호 개수만 세므로 `A[검사 (25ms)]`처럼 짝이 맞는데 파서가 거부하는 라벨을 볼 수 없었다. 실패 원인 네 가지(따옴표 없는 노드·엣지 라벨의 괄호, `subgraph` 제목의 괄호, 제목을 노드 ID로 참조, `end` 누락)를 라벨 텍스트를 읽는 방식으로 검출하고, 실제 파서와 교차 대조해 오탐 0·미탐 0을 확인했다. `&`·`#`·`%`·`$`·`/`·`<br>`·한글과 `sequenceDiagram`의 괄호는 파싱되므로 규칙에서 제외했다. `note-writer`에는 검사 실행 단계가 아예 없어 깨진 다이어그램이 통과하던 경로였다 |
| 2026-07-25 | 텔레그램 봇 브리지 공식 레퍼런스 템플릿(`examples/telegram_bot/`) 제공 | `examples/telegram_bot/`, `docs/guides/mobile-telegram-bot.md` | 비추적 디렉터리(`_workspace/`)로 인해 깃 복제 시 봇 스크립트가 유실되거나 LLM이 처음부터 코드를 재구성해야 하는 번거로움을 해결하기 위해, 검증된 `bot.py` 및 `run_bot.sh`를 공식 예제 템플릿으로 제공하여 1초 만에 복사하여 실전 구동할 수 있도록 환경 구축 |
| 2026-07-25 | 텔레그램 봇 맥(macOS) 부팅 시 자동 구동(`launchd` LaunchAgent) 구성 및 문서화 | `~/Library/LaunchAgents/com.nohdol.telegrambot.plist`, `docs/guides/mobile-telegram-bot.md` | 컴퓨터 재부팅 시마다 수동으로 터미널에서 환경변수 및 백그라운드 명령어를 입력해야 하는 번거로움을 해결하고, 예기치 못한 종료 시에도 OS가 즉시 재구동(`KeepAlive`)하도록 `launchd` 자동 시작을 구성 (보안 계율 RULE 5 준수를 위해 토큰 설정은 프로젝트 외부의 사용자 개인 계정 경로에 안전하게 격리) |
| 2026-07-25 | 텔레그램 봇 브리지 MessageEntity 기반 렌더링 및 로컬 URL 정제 탑재 | `_workspace/telegram_bot/bot.py`, `docs/guides/mobile-telegram-bot.md` | `MarkdownV2` 문자열 파싱 시 클라이언트 환경에 따라 백슬래시(`\`)나 별표(`*`)가 노출되거나 API가 `file://` 링크를 미지원 프로토콜로 거부하는 문제를 완벽 해결하기 위해, 순수 텍스트와 스타일 속성 배열(`MessageEntity`)을 분리 전송하는 모드로 전면 개편하고 웹 외 URL을 인라인 코드로 사전 정제하는 방어 로직 탑재 |
| 2026-07-25 | 텔레그램 봇 브리지 마크다운 렌더링 고도화 (`telegramify-markdown` 적용) | `_workspace/telegram_bot/bot.py`·`run_bot.sh`, `docs/guides/mobile-telegram-bot.md`, `docs/README.md` | 텔레그램 Bot API의 엄격한 마크다운 규격으로 인해 AI CLI 응답이 평문으로 출력되거나 `**[Menu]**` 등에서 파싱 오류(`BadRequest`)가 발생하던 문제를 해결하기 위해 표준 마크다운을 `MarkdownV2`로 자동 이스케이프 및 안전하게 분할하는 `telegramify-markdown`을 탑재하고 예외 시 평문 폴백 방어 로직을 문서화 및 반영 |
| 2026-07-25 | Understand Anything NPM 의존성 상시 자동 설치 승인 반영 | `study-install`, `install-phase2b-tools.sh`, `understand` SKILL·adapter-contract, 한글 스킬 안내 | 사용자의 상시 설치 승인 결정에 따라 Node 22+ 및 pnpm 10+ 환경에서 `study-install` 실행 시 `@understand-anything/core` 및 dashboard NPM 의존성이 자동 설치·빌드되도록 규칙 및 스크립트 수정 |
| 2026-07-25 | Phase 1 파일 기반 공부 하네스 구축 | 저장소 골격, 설치기, 스킬, 훅, 문서 | 설치처별 vault와 Claude Code·Codex를 연결하면서 지식과 하네스를 분리 |
| 2026-07-25 | 주장 단위 근거 검증 규율 추가 후 상시 규칙으로 통합 | AGENTS, `note-writer` 필수 참조, 노트 스키마 | 선택 스킬 활성화 누락 없이 잘못된 정보의 확정 지식화를 방지 |
| 2026-07-25 | Phase 2 ingest·NotebookLM·그래프 기준 구현 | 웹 capture, 논문·영상 스킬, 검증 export, 결정적 그래프, 설치 도구 | 다양한 자료를 원문 보존·검증 경계를 유지하며 학습 자산으로 전환 |
| 2026-07-25 | metaskill 규칙 도입과 스킬 문서 보강 | `metaskill`, 루트 README, 한글 스킬 안내, 기존 스킬 | 하네스 변경과 사용자용 라우팅 문서가 함께 검증·동기화되도록 함 |
| 2026-07-25 | direction 전체 재검토와 Phase 2b 범위 수정 | Understand Anything 9개 스킬, Obsidian 4개 스킬, NotebookLM CLI, ADR 003, 보안 검토, 실행 스펙 | 다른 프로젝트의 판정 대신 nohdol-study의 코드·도메인·설계·지식 학습 목적만으로 평가하고 project-local 설치 경계를 명문화 |
| 2026-07-25 | 추가 도구 후보 재검토 | basic-memory, PaperQA2, Graphiti, Mem0, Cognee, Kuzu, Obsidian REST/MCP, SR, diagram | 임의 100노트 게이트를 제거하고 실제 효용·source-of-truth·외부 전송·운영 비용에 따라 채택과 재검토 조건을 구분 |
| 2026-07-25 | 다음 세션 작업 인계와 Phase 경계 정합 수정 | Phase 2b-A~E, Phase 2c·3, Physical AI 이미지, AGENTS 한글 뷰, `knowledge-graph` | 새 세션이 설치처 정보나 외부 전송 경계를 추측하지 않고 바로 이어서 구현하도록 하고 남아 있던 basic-memory 100노트 규칙을 현재 결정과 일치시킴 |
| 2026-07-25 | `notebooklm-export` 스킬 제거 (ADR 004) | 스킬 전체, `study-install` NotebookLM 모드·`bootstrap.sh --notebooklm`, REGISTRY 필드, AGENTS·CLAUDE·README·한글 안내·MOC·스펙 | 763줄을 유지했으나 실사용 패킷 1개, 생성물 회수 0건. 매니페스트·해시는 '나중에 되짚기'를 위한 장부인데 되짚는 행위가 없으면 아무것도 보증하지 않는다. CLI bridge는 릴리스 게이트에서 이미 차단 판정. 'AI 출력은 독립 근거가 아니다' 규칙은 브라우저 직접 사용에도 유효하므로 유지 |
| 2026-07-25 | Phase 2c basic-memory 파일럿 실행·미채택 | 2c 현황 검토 문서, MOC | 사용자 지정 corpus(`vault/공부`)로 실행. 원본이 아닌 scratch 사본에서 돌린 판단이 결과를 갈랐다 — 색인이 마크다운 30개의 frontmatter를 전부 새로 써 읽기 전용 전제를 구조적으로 충족할 수 없음이 드러났다. 검색 품질과 로컬 임베딩은 양호하므로 기각이 아니라 '소유권 대가를 사용자가 받아들이면 레거시 검색용으로 유효' 판정 |
| 2026-07-25 | Phase 2c 전제 실측과 파일럿 하네스 | `pilot.py`·테스트, `knowledge-graph` 스킬, 2c 현황 검토 문서, MOC·한글 안내 | 진행 요청을 받고 전제를 재보니 PaperQA2는 provider·corpus가 둘 다 없어 실행 불가, basic-memory는 비교할 corpus가 사용자 결정 사항이었다. 돌릴 수 없는 도구의 절차를 지어내는 대신, corpus와 무관하게 유효한 안전 장치(실행 전후 해시 대조로 실격·쓰기 명령 사전 차단)를 먼저 구현 |
| 2026-07-25 | Phase 3 — 다이어그램·학습 대화·정원 가꾸기·복습 | `diagram`·`study-session`·`vault-gardening`·`recall` 스킬과 검사 스크립트 3종·테스트 3종, AGENTS·README·한글 안내·MOC | 말로만 둔 규칙은 지켜지지 않으므로 판정 가능한 것은 스크립트로 옮겼다 — 다이어그램 승급은 노드 수로 세고, vault 드리프트는 계약 위반 단위로 보고하며, 복습 카드는 인용 노트에서 앵커가 해석될 때만 통과한다. 파생물이 근거가 되지 않는다는 경계를 넷 모두에 공통으로 실었다 |
| 2026-07-25 | Phase 2b-E — NotebookLM 릴리스 게이트와 업로드 전 packet 검증 | `bridge-gate.sh`·`verify-packet.sh`·`bridge_test.sh`, `notebooklm-export` 스킬·`export.sh` slug 검증, README·한글 안내·MOC·스펙 | 재감사 결과 최신 안정 릴리스 v0.7.3에 redirect 수정이 없음을 코드로 확인(가드 모듈 404)해 설치·인증·전송을 열지 않았다. 대신 판정을 결정적으로 재현하는 게이트와, 수동 업로드에도 당장 쓸모 있는 packet 재검증을 구현 |
| 2026-07-25 | Phase 2b-D — Obsidian 형식/CLI 스킬과 형식 검증기 | `obsidian` SKILL·`references/modes.md`·`scripts/validate.py`·테스트 2종, README·한글 안내·MOC·스펙 | Obsidian 고유 문법은 기억으로 쓰면 겉보기엔 맞고 조용히 렌더링에 실패한다. 형식 세 가지는 앱 없이 검증 가능하므로 검증기를 두고, 콜아웃 목록은 pin된 참조에서 읽어 상류와 동기화한다. `.base`는 무의존 원칙상 YAML 검증이 불가해 구조 사전 점검임을 명시 |
| 2026-07-25 | `understand` 9개 스킬을 단일 스킬 내부 라우팅으로 통합 | `understand` SKILL·`references/modes.md`·`routing_test.py`, 제거된 8개 스킬 디렉터리, AGENTS·CLAUDE·README·한글 안내 | 사용자 결정. 스펙 R2가 요구한 것은 9개 entry point 제공이지 9개 스킬이 아니며, 경계를 아홉 번 반복하면 서로 어긋난다. 라우팅 표는 SKILL에, mode별 절차는 references에 두어 스킬 수를 20→12로 되돌림 |
| 2026-07-25 | Phase 2b-C — typed 지식 그래프와 근거 검증 추론 계층 | `build_graph.py`, `knowledge-graph` 스킬, `understand-knowledge`, AGENTS, README·한글 안내·MOC·스펙 | 노트가 1개뿐인 지식 루트에서도 그래프가 쓸모 있으려면 링크 말고도 주제·출처가 노드여야 했다. 동시에 모델 추론이 사실과 섞이지 않도록 추론 계층을 opt-in으로 분리하고, 근거 앵커가 인용 노트에서 해석될 때만 받아들이며, 노트 본문은 파생물에 담지 않는다 |
| 2026-07-25 | Phase 2b-B — Understand Anything adapter 9종 | `understand`·`understand-chat`·`-dashboard`·`-diff`·`-domain`·`-explain`·`-figma`·`-knowledge`·`-onboard`, 공통 `adapter-contract.md`, adapter 정합 테스트, AGENTS·CLAUDE·README·한글 안내·MOC·스펙 | 상류 스킬을 그대로 열지 않고 이 하네스의 경계(그래프는 근거 아님·산출물 격리·실행별 승인) 안에서 노출하기 위해. 조사 결과 한 pin 안에 런타임이 세 계층으로 갈려 있어, 배치는 아무것도 실행하지 않으므로 게이트를 설치 시점에서 실행 시점으로 옮김 — 이로써 의존성 없이 도는 `understand-knowledge`가 열림 |
| 2026-07-25 | Phase 2b-A — 외부 소스 exact-pin 설치 기반 | `.tools/PINS.md`, `install-phase2b-tools.sh`, `tree_hash.py`, `study-install`, `.gitignore`, README·한글 안내·MOC·REGISTRY | upstream 전역 installer 없이 정확한 commit만 재현 가능하게 배치하기 위해, 아카이브 바이트가 아니라 트리 내용으로 신원을 고정하고 hash·tag·runtime·pin 형식을 모두 fail-closed로 막음 |
| 2026-07-25 | 독립 리뷰 지적 반영 — 훅 정지 계약, 링크 해석, 설치 관측, 스킬 경계 | `study-wrapup`, `build_graph`, `web-capture`, `bootstrap`, `install-phase2-tools`, `defuddle`·`context7`·`paper-search`·`notebooklm-export`·`knowledge-graph`·`ingest`·`metaskill` 스킬, AGENTS | Stop 훅이 강제 continuation에서 무한 차단되던 경로를 막고, 파일명·NFC 링크 해석과 설치 성공 관측을 실제 동작에 맞추며, 이식 과정에서 빠진 폴백·경계·주입 방어를 복원 |
| 2026-07-25 | 루트 README 리팩토링 및 모바일 텔레그램 스터디 브리지 추가 | 루트 README, docs MOC, `docs/guides/mobile-telegram-bot.md` | 루트 README에 기술적 세부 구현이 과도하게 노출되어 있던 점을 개선하여 개요·빠른 시작·스킬 맵 중심으로 정돈하고, Mac 하네스와 폰 텔레그램을 안전한 화이트리스트 기반으로 연결하는 비동기 봇 브리지(`_workspace/telegram_bot/`) 및 사용자 가이드를 공식 추가 |
