# nohdol-study harness change history

| date | Changes | Target | reason |
|---|---|---|---|
| 2026-09-11 | Explain why every glossary term matters | 403 chapter definitions, 14 core explainers, bilingual extraction, dialog, tests and navigation | Add problem-specific reasons and use situations in English and Korean, preserve domain-specific meanings, and reject incomplete purpose copy before publication. |
| 2026-09-11 | Add bilingual article reading and in-context terminology | All 94 selected translations, parallel renderer, reading controls, terminology, search, publication tests, rules and navigation | Let readers learn technical content and English together with collapsible Korean explanations, shared examples, and explicit definitions. Bind translations to reviewed source versions and preserve evidence dates, links and execution fixtures. Expand CDC and dbt introductions with purpose and role comparisons. |
| 2026-09-11 | Add a Korean/English documentation interface switch | Header controls, navigation translations, local preference storage, tests, specification, and navigation | Let readers switch the current view and retain their choice across reloads. Translate all path and topic introductions and interface controls; identify English article text explicitly and preserve canonical content, code, routes, and publication gates. |
| 2026-09-10 | Expand the Data course's individual technical mechanisms | All 17 course chapters, source coverage map, depth specification, local example verifier and navigation | Replace compressed topic lists with SQL/runtime, storage/protocol, Spark/Kafka, dbt, quality, OTel/backend, governance, managed-platform and AI explanations. Add reproducible calculations, SDK propagation, incremental-model verification and concrete cloud exercises while preserving publication routes and evidence boundaries. |
| 2026-09-10 | Review the full documentation set and add lab result examples | Learning chapters, operational guides, historical status notices, review specification, tests, and navigation | Correct transaction, lifecycle, networking, authorization, telemetry, and data-publication explanations; make expected outputs and failure/recovery verdicts reviewable; distinguish local execution from unrun environment-dependent labs. |
| 2026-09-10 | Add the Data & Observability learning path | Seventeen English chapters in `docs/guides/data-observability/`, explicit catalog entries, adaptive path cards, tests, source review, specification, and navigation | Build on the user-selected shared conversation with distributed data, quality, OpenTelemetry, lineage, governance, managed platforms, AI evaluation, and one evolving capstone. Add executable local correctness fixtures and explicit recovery/measurement boundaries. Preserve existing publication gates and routes; the new course is selected explicitly rather than copying private notes or publishing all of docs/. |
| 2026-09-07 | Translate repository documentation and the public learning gateway into English | All 77 public articles, catalog, site controls, READMEs, guides, ADRs, specs, reviews, rules summary, skill guide, and documentation language policy | Translate reader-facing prose while preserving source records, executable examples, document IDs, links, and functional Korean skill-trigger aliases. Retain historical `.ko.md` filenames for link compatibility. Extend the build regression test to require English page metadata, controls, catalog, and article output. Validate source/code/link preservation, the site build, harness consistency, all 101 rendered diagrams, search, navigation, the diagram viewer, and mobile layout. Translation does not advance existing evidence-check dates. |
| 2026-09-04 | **Enhancing the purpose, scope, and usage of Kubernetes Namespace in public learning documents** | Kubernetes roadmap·`02. API와 오브젝트`, public catalog·build test | The existing Chapter 02 briefly mentioned Namespace only as a name range, making it difficult to know when to use it and what was not isolated by Namespace alone. The scope of application of name·lookup·RBAC·policy·quota·Service DNS is explained in a relationship diagram, and namespaced/cluster range lookup, YAML for creating ConfigMap of the same name in two namespaces, use of `-n` and context default values, full lookup, and safe cleanup procedures were added. The namespace itself is not a security boundary, and the operational boundaries specified that RBAC·NetworkPolicy·Pod Security·ResourceQuota·LimitRange are separately required, and are maintained in public builds through catalog summary and regression inspection. |
| 2026-09-03 | **Separate the public document root into DevOps and AIOps and add 30 documents including the entire backend and AI map** | `docs-site` path catalog·UI·verification, 3 Traffic & Resilience, 7 Backend Engineering, 6 AI Specialist, 5 AI Transformation, 9 AIOps operation closed loop documents, ADR 009·two learning spec·guide·root README | The existing `Infra Specialist` name was changed to `DevOps`, which covers back-end operation and deployment life cycle. The root divides DevOps 15 topics and 57 documents and AIOps 5 topics and 20 documents, and the build rejects missing, duplicate, and missing references so that all topics belong to exactly one path. Vault was used only to find topic maps and connection gaps, and public statements were checked again in RFC, PostgreSQL, Kafka, Kubernetes, OpenTelemetry, NVIDIA, MCP official documents and the original paper. In DevOps, Traffic & Resilience and backend API → transaction → capacity → distributed workflow → cache → compatible deployment were added. AIOps includes AI Specialist's LLM·Vision/Generation·On-device·Time Series/Recommendation·RAG/MCP and AI Transformation's GPU·MLOps/LLMOps·AI DevOps/FinOps·enterprise agent and connects incident evidence → diagnosis → approved recovery. The overall content connection table and build regression test of the three roadmaps prevent missing topics for 81 backend, 49 AI Specialist, and 50 AI Transformation vault items. |
| 2026-09-03 | **Reorganize Infra Specialist documentation from beginner to expert judgment** | Roadmap·concept·lab document of 12 new topics, catalog·site introduction·build test, public learning path spec·public guide·docs MOC·root README | The existing document used terms such as `state binding`, `MVCC`, and `failure boundary` from the first paragraph, making it closer to a compressed reference for readers with infrastructure experience. We set the target audience as application developers with only basic shell experience, and added familiar problem situations, easy terminology guidance for learning, and beginner confirmation questions and answer standards to each roadmap. The concept chapter defines key terms before the detailed model, and the lab chapter first announces the necessary tools, startup status, files, permissions, and organization targets. The catalog summary was also changed from listing product terms to observations and actions that readers will actually take. The test checks whether this beginner's entry structure exists before the detailed description. It was stated in the public guide that easy meanings and scenarios are not direct quotations of official sentences, but comprehensive explanations for learning official facts. |
| 2026-09-03 | **Infra Specialist concept·lab Explanation connection reinforcement of 24 documents** | `01-*`·`02-*` documents of 12 new topics, public learning path spec·build test, public guide·docs MOC·root README | Users pointed out that the existing document should be kinder in terms of content rather than wording. The official source and operational checklist were maintained, but boundaries for key terms that appeared for the first time in each concept chapter and one specific request/failure situation were added, and a commentary was added to each lab chapter distinguishing the purpose of the command, what the results prove and what they do not yet prove, and partial successes that appear normal. The test checks whether 24 detailed documents have `먼저 이해하기` context/comparison table and 12 lab documents have `결과를 이렇게 읽는다` commentary and minimum amount of explanation to prevent regression that leaves only a checklist. |
| 2026-09-03 | **Infra Specialist public route implementation of 13 topics and 47 documents** | `docs-site/content/` 12 new topics·36 documents, catalog·UI·build tests, Infra spec·ADR 008·docs MOC·open guide·root README | According to the pre-implementation specifications, a capstone of Linux·network·AWS base, Terraform·Helm/GitOps declaration·deployment, Observability/SRE·security operation, PostgreSQL·Redis/DynamoDB·messaging data layer, reliability·DR·FinOps, and integration capstone was created, and Karpenter was released as the only in-depth topic. The new topic is completed in three documents: roadmap, operating model, and lab, and includes mental model, Mermaid relationship diagram, failure/recovery, production trade-off, and review questions. AWS changes were divided into Local, Plan only, and AWS optional, and temporary credential, cost, resource inventory, and cleanup were specified. The catalog was expanded in order of prerequisites and the first screen was changed to the entire Infra Specialist path, and the test examines 13 topics, 47 Git tracking documents, internal routes, non-exposed source comments, diagrams, and execution examples. |
| 2026-09-03 | **Specification confirmed prior to implementation of Infra Specialist public learning path** | New `docs/specs/2026-09-03-infra-specialist-public-learning-path.md`, ADR 008·docs MOC·open study guide·root README | The user requested that Helm Charts, Terraform on AWS, PostgreSQL, and NoSQL be added as public processes following Kubernetes and that a full document production specification be configured first. Instead of immediately increasing the product list, we fixed the prerequisite relationships and topic responsibilities that lead to declaration/deployment, observation/SRE, PostgreSQL/Redis/DynamoDB, security/messaging/DR/FinOps based on Linux, network, and AWS. Each chapter is equipped with mental model, execution and observation, failure/recovery, production trade-off, and official source metadata, and AWS lab follows local·plan-only·AWS optional level, cost, permission, and cleanup contract. Empty cards are not made public and are registered in the catalog when each step-by-step topic is completed. The initial in-depth product included only Karpenter, as determined by the user, and explicitly excluded Ansible/Packer, Cilium/eBPF, service mesh, Vault, Terraform alternatives, Chaos products, and additional NoSQL products. This change only added plan specifications and navigation links and did not change the behavior of the public learning text, catalog, or site. |
| 2026-09-03 | **Convert externally linked table of contents to internal, complete learning document and add Mermaid rendering** | 11 Kubernetes documents, `docs-site/build.mjs`·Browser app·style·test, Mermaid 11.17.2 lock, docs-site guide·ADR 008·public docs spec·docs MOC·root README | Rather than redirecting the provided link back to the reader, users want to reorganize the content into easy-to-understand text, relationship diagrams, sequences, and detailed examples within the site. The `공식 시작 링크` list of 11 documents was removed, and the evidence URL and confirmation date were moved to HTML comments that disappear in the build. The first input, the Kubernetes document home, was expanded into a full roadmap with declaration → observation → coordination model, functional/non-functional boundaries, deployment sequence, and internal learning sequence. The first cluster chapter was completed to run minikube, Deployment/Service YAML, request path, status observation, Pod deletion self-healing, and `ImagePullBackOff` diagnosis and recovery of incorrect image in one document. Markdown's Mermaid block renders with a fixed-version browser bundle included in the artifact without an external CDN, and the test verifies that there are no Kubernetes external links in the public HTML and that the diagram target and bundle exist. |
| 2026-09-03 | **Current public learning target set to Kubernetes and table of contents composed of 11 documents** | `docs-site/catalog.json`·New `docs-site/content/kubernetes/` 11 documents·Site UI and testing, docs-site guide·ADR 008·public docs spec·docs MOC·root README | Your goal is not to teach nohdol-study itself, but to create an open document that makes it easy to learn your chosen technology topic. The gateway will support multiple topics in the future, but the current catalog removes the existing six harness topics and exposes only one Kubernetes card. The official Kubernetes Korean documentation's installation, concepts, tasks, tutorials, and references have been reorganized into 11 documents in the `왜 필요한가 → API와 제어 루프 → 워크로드 → 네트워크·스토리지 → 스케줄링·보안 → 장애 대응·운영과 확장` order. Afterwards, the link provided by the user adds a one-sentence model, relationship diagram, minimum execution example, detailed explanation, failure/recovery, operational judgment, source/confirmation date, and review questions to the relevant chapter. New topics are added as independent content directories and catalog cards. If there is a translation delay warning on a Korean page, check the current English document and references. `docs/` and private `vault/` are not automatically used as public curriculum. |
| 2026-09-03 | **Add public document gateway and GitHub Pages deployment paths by topic** | New `docs-site/`·`.github/workflows/docs-pages.yml`, `AGENTS.md`·`AGENTS.ko.md`, ADR 008·public docs spec, docs MOC·root README | We created a gateway for 6 topics and 20 documents, full search, reading screen, dark mode, and mobile layout so that the repository's ADR, specifications, and guides can be read as a learning path rather than a file list. Actual knowledge roots are excluded from Git and may contain personal information, so they are not automatically scanned. The disclosure target is limited to Git tracking Markdown written in the catalog, and the build rejects `vault/`, `REGISTRY.md`, `_workspace/`, path deviation, and untracked sources. The product is not tracked and only deployed as a Pages artifact. Confirmed through 4 types of Node tests, static build, local HTTP 200, and headless Chrome desktop and mobile visual review. Users were always authorized to commit and push verified general changes to `origin/main` without separate confirmation and to check Pages deployment. Excludes force push, history rewrite, release, other remotes and branches. Actions run `33703435234`'s build·deploy were all successful, and <https://nohdol97.github.io/nohdol-study/> and the three core assets returned HTTP 200. |
| 2026-08-16 | Integrate dynamic HTML sites for users into a single `_workspace` portal | `AGENTS.md`·`AGENTS.ko.md`, new `examples/workspace_portal/`, ADR 007·portal spec, docs MOC·root README | When Robot AI Academy was made into an independent directory/dedicated server, the problem of ports and processes increasing each time the site increased was revealed. Only the user site is registered in `_workspace/sites/<slug>/` and explicit `sites.json`, and `_workspace` is served only once. Internal analytes are not automatically exposed. The manager does not overwrite existing portals and rejects entries without traversal paths or slugs. Verified through temporary workspace regression testing and actual portal check. |
| 2026-08-09 | **Add archify as a skill dedicated to explicit calls, but separated from the note pipeline** — Register pin (`v2.13.0`), create new adapter skill, add `node18` runtime value to installer, specify boundary with `diagram` | New `.agents/skills/archify/SKILL.md`·`docs/adr/006-archify-explicit-use-outside-vault.md`, `.tools/PINS.md`, `install-phase2b-tools.sh`·`_test.sh`, `.agents/skills/diagram/SKILL.md`, `AGENTS.md` Section 8·`AGENTS.ko.md`, `README.md`, `.agents/skills/README.ko.md`, `docs/README.md` | User requested review of introduction of <https://github.com/tt-a1i/archify>. The tool itself was fine — MIT, external transfer 0, schema/test/CI present. What is blocked is **the shape of the output**. 🔥 **Critical fact: There is no SVG output path in the CLI** — The command for `bin/archify.mjs` is `render/compare/deliver/preview/validate/inspect/check/guide/examples/doctor/demo`, and there are no `.svg` strings in the entire file. "PNG/SVG export" in the README is a **viewer button when the generated HTML is opened in a browser**, and is not a command that can be called. This, coupled with the fact that Obsidian does not embed HTML, and the output is about 600 KB, and the knowledge root is cloud synchronization, there is **no way** to include it in the notes (there is no bypass). So, I adopted it, but isolated it as `_workspace/archify/`, and wrote in both skills that complexity is not a routing reason — promotion is still Mermaid → D2. ⚠️ Upstream recommended installation method `npx skills add -g` violates Section 1 of `AGENTS.md`, so only the pin path is used. The reason for adding the new `node18` value is for honesty — if you write this tree as `node22-pnpm10`, the installer reports that **pnpm is not available**, but the declared dependency of archify is one `ajv` for dev and the validator is committed, so the description is false and the user installs pnpm according to that lie. **Verification**: Passed overall, including 2 new installer regressions (`node18` met in v18, not met in v16), `verify_harness.py` passed. After actually placing the pin and verifying the tree hash, we checked `ready`, and `doctor` passed 14 items with exit 0 **without installing dependencies** — we actually measured that the value of `node18` was correct. The `demo` output was **one 597.6KB HTML**, and it was confirmed that the capacity included as isolation evidence was not an estimate. ⚠️ **Side effect**: `--install` does not end with just archify — if the block at the end of the installer contains node·pnpm and `understand-anything/package.json` exists, **even if the tree has already been deployed**, `pnpm install` and `pnpm -r build` are rebuilt and even the dashboard is rebuilt. Since it was an existing motion, I did not touch it this time. ⚠️ No visual review — we only saw file existence and size, did not open it in browser, so we make no claims about render quality |
| 2026-08-05 | Correct the target path of 6 types of feeds from `Robotics/` to `Physical AI/` and add a guard **to stop instead of creating a new queue on the old route** | `examples/feed_scraper/scrape.py` (new `find_note_elsewhere`, `scrape_feed_source`)·`scrape_test.py`, operating copy `_workspace/feed_scraper/scrape.py` | While measuring the number of notes in `index.md`, I found that 5 notes in `wiki/Robotics/` **all were created today** and that there are already 5 notes in `wiki/Physical AI/` with the same name. `scrape.py` is holding `'path': "Robotics/..."`. **In the 2026-08-04 folder cleanup, the source of truth was moved and the scraper path was not followed.** 🔥 **There is no signal in this failure mode** — The scraper creates the file if it does not exist, and Obsidian resolves the wiki link by name, so the link is not broken, and the cumulative copy remains where it was moved, so no operation fails. What `vault-gardening` caught was only **1 duplicate title** and there were 0 broken links and orphans. It is in the same series as the GeekNews incident on 2026-08-03, but at that time it was **deployment** (the operating copy was out of date) and this time it was **deployment** (the vault was moving), so it cannot be caught by the hash comparison entered then. **If you only change the path constant, it will be repeated in the next folder cleanup.** So, if the target file does not exist and the same name `wiki/` is found somewhere else, **it will not be created, but will be skipped and the path found will be taken** — In a new installation (nowhere else), it will be created as before, so it will not block the first run. Scan only scans the curation layer and adheres to AGENTS Section 8. ⚠️ **Organization order was important** — `Robotics/` had fewer items (past items pushed out of the feed are only found in source of truth), but **4 of today's collections were only there.** Checked with marker difference, merged the 4 items (IEEE 1, The Robot Report 3) into the source of truth month section by date, re-confirmed that `Robotics/` was a true subset, and then deleted the folder. erased **Verification**: Passed 3 new regressions (do not create new queue on old path, do not touch moved notes, create as usual if path is correct), passed all existing tests. In the mutation that disabled the guard with `misplaced = None`, we confirmed that the first item was **FAIL** to verify that the test actually catches the regression. Copied it to the operational copy and confirmed it is the same as `cmp` |
| 2026-08-05 | Fixed embed asset path to be interpreted as vault relative instead of only looking in note directory — `wiki/<주제>/` note now recognizes actual batch embedding `wiki/assets/` | `diagram/scripts/check.py` (New `embed_resolves`)·`check_test.py`, `diagram` SKILL "Rendering to SVG" | While taking notes on the in-house agent-eval wiring document, I promoted the 19-node flowchart to D2 and embedded it as `![[assets/agent-eval/wiring.svg]]`, but the `PostToolUse` hook blocked saving with "embedded asset not found." The file was normally located at `wiki/assets/agent-eval/wiring.svg`. The reason is that the checker only sees two candidates, `path.parent / target` and `path.parent / "assets" / name`, **Obsidian wikilink embeds are interpreted across the entire vault, not the note's directory.** 🔥 **False positives weren't just a problem with new notes** — A scan of the actual vault showed that `AI Transformation 핵심 엔지니어링 위키` (`assets/ax-wiki/ecosystem.svg`) and `NVIDIA A100 4장 기반 사내 AI LLM 통합 플랫폼 구축 가이드` (`assets/a100-ai-platform/architecture.svg`) **already had the same false positive status**, and both notes are rendered fine in Obsidian. In other words, this was a condition in which the tester never actually detected anything broken. The drift time appears to be 2026-08-04 — the relative positions of the notes and `assets/` were changed as the 69 notes scattered in the root were moved that day to 8 topic folders (depth 1), but the checker's path assumption remained the same as the flat layout. **Walk upward, but not indefinitely**: Stop when it encounters `wiki/`, and break in three steps for batches without such a name (such as test temp directories) — without an upper bound, a file with the same name somewhere upstream **will be a false positive in the opposite direction, passing as an asset that isn't there**. **Verification**: Two new tests (pass if `wiki/AI Transformation/hub.md` embeds `wiki/assets/ax-wiki/ecosystem.svg`; files that are not actually present in the same batch are still detected as exit 1), all existing tests pass. 2 false positives in actual vault → 0, new note saved successfully. SKILL's phrase "Place the source in `assets/` next to the note" was also corrected as it was inconsistent with actual practice (`wiki/assets/<그룹>/`). |
| 2026-08-03 | The execution wrapper compares each execution trace with the SHA-256 of the production copy, and if there is a discrepancy, a warning is issued at the beginning of the log. | `examples/feed_scraper/run_scraper.sh`, new `run_scraper_test.sh`, `examples/feed_scraper/README.md`, `docs/guides/feed-scraper.md` | A user reported “8-2 geeknews location was created incorrectly.” The cause was not the code, but **deployment** — launchd was calling the untracked `_workspace/feed_scraper/scrape.py`, which was copied on 2026-07-26, and the commit `0b41a3c` (separating the output into two layers `raw/`·`wiki/`) from a day ago was not copied. Older versions only know `VAULT_DIR = wiki/GeekNews`, so the 08-03 06:00 run created the entire 08-02 minutes in `wiki/` — 1 daily report, 1 monthly index (`raw/` already had the same name, making the `[[2026.8 인덱스]]` wikilink ambiguous), and the obsolete `주제/` subfolder. 1 topic document. **The defect that was fixed yesterday was reproduced and the logs were saying it was normal.** The point is that it was a silent failure — a person had to see it with their own eyes to see that a file had been created in the wrong location. The price of this structure is that “the engine is in two places,” but the guide only guided copying during installation **there was no re-copying procedure after changing the code**. The user specified the direction to fix it ("Compare hashes in run_scraper.sh and warn me"). The objects of comparison are only the three that determine the execution behavior (`scrape.py`·`run_scraper.sh`·`requirements.txt`) — Even if `README.md` and `sources.local.example.toml` are separated, the collection results do not change, but if the warnings become more frequent, the warnings themselves will no longer be read. **Doesn't stop even if it's misaligned**: Running with old code is better than losing collection, and since there are cases where the copy side is modified first, a short hash on both sides and two direction guides are recorded so that the copy direction can be determined by a person. If the trace is not visible (a copy has been removed and placed elsewhere), the check is skipped — this is to avoid breaking the existing design, where the wrapper derives all paths from its own location. **Verification**: 10 types of new `run_scraper_test.sh` (silent if identical, passes inspection, reaches venv confirmation, points out engine misalignment, accompanied by command to fix, does not report untouched files, continues execution even if misaligned, resolves when correct, misalignment of the wrapper itself, silence in the absence of trace copy, silence in self-comparison in place of trace copy) Passes, confirms that it fails in the mutation that empties `SYNCED`, so the test is completed I checked to see if it actually caught regression. In the operational copy, both normal silence and deviation warning were confirmed by actual execution. Together, we cleaned up three invalid vault files — moving the daily report to `raw/geeknews/2026.8/`, regenerating two as `refresh_month_index` after deleting duplicate month indexes, and merging one 8-02 entry from the subject document (`gn:32043`) into a new `## 2026-08` section in source of truth `wiki/GeekNews/시스템과 데이터베이스.md` (after checking for absence of duplicate markers). Remove `주제/` folder |
| 2026-08-03 | Downgraded the frame extraction of `study-video` from the default 2-pass to **Conditional Confirmation** and changed the frame to not be stored in the vault. | `study-video` SKILL(description·Why·Procedure 5·7·8·With/without), `.agents/skills/README.ko.md`, root `README.md` | While looking for a cost-saving axis, the user specified that "video frames are not needed in the document." There was actual evidence — I received the entire **65MB video the previous day to extract 4 frames from the notebook.** If you give `--detail transcript`, you only receive subtitles but no video, but the moment you add `--timestamps`, the entire download is included. However, the existing procedure listed pass 2 as the **basic step**, so this fee was added to each video. 🔥 **The key point is that frames have two uses** — as illustrations for notes, and as a means of verifying the speaker's numerical claims. The value of the previous day's frame was the latter (confirming that “6 times” and “0.4%” are concept diagrams without axes and units), and **the verification result remains as a sentence, not an image.** If “there are no axes, units, or measurement conditions” is entered into the verification table, the image only conveys the same fact again to the person who opens it. So, I reversed procedure 7 from “Copy to vault” to “Write the verification result as a sentence in the verification table and discard the frame along with the working directory.” The only exception is when **the claim itself is cosmetic** (diagram O being explained, number X being audited). Step 5 requires **the screen to first name the question it will answer** before execution — pass 2, which has no payoff, leaves only the download cost. ⚠️ **`--download-sections` stated that it is not used as a solution** — In the cut file, 0 seconds is the original T seconds, so if you extract the frame with the original timestamp, **quietly the wrong point** comes out. The savings method was not adopted because it was a tradeoff that resulted in a wrong answer. The two frames that were already embedded in two notes from the previous day were left as is because the verification table cited them as evidence — the rule change does not retroactively cut off evidence that has already been established. |
| 2026-08-02 | `hot.md` budget increased from 2000 → 3000 bytes, and the scattered "500 token" notation was unified with the actual gate value. | `vault-gardening/scripts/garden.py`(`DEFAULT_HOT_BUDGET`)·`garden_test.py`, `AGENTS.md`·`AGENTS.ko.md` Section 2, `docs/specs/2026-07-25-phase1-study-harness.md`, `note-writer`·`using-study` SKILL | After recording 6 types of harness videos, I updated `hot.md`, but even after reducing it by 44% (3841 → 2167), it exceeded the budget, and the user asked, "Isn't the budget too tight?" **When I measured it, it was the opposite of what I expected** — `hot.md` is 3.30 bytes per token, and English `AGENTS.md` is 4.00 bytes. A Korean syllable is 3 bytes, but it is roughly 1 token, so both bytes and tokens increase, and **Korean is not disadvantageous in terms of byte budget, but has a 20% advantage.** So, “it is tight because it is Hangul” is not upward evidence. The actual evidence was separate — **The source of 2000 is `docs/proposals/2026-07-25-nohdol-study-direction.md`'s "~500 tokens", and that is the value when the vault was 30 notes.** Now, with 160 notes and 7 hubs, the topic that the cache has to guide has increased by five times, but only the constant has remained the same. The actual damage has already occurred: the transfer of four rules in the 2026-07-25 item in this table occurred because it exceeded 4002 bytes. ⚠️ **The unit was kept in bytes** — If you change it to a token approximation, it is different for each tokenizer **A number that no one can recalculate enters the gate.** Instead, I added a conversion ratio (Korean 3.3 / English 4.0) and "Why 3000" as a comment next to the constant, and made AGENTS.md and the gate say the same number by adjusting the notation in 5 places in the document based on bytes. The test expanded from only looking at one case exceeding 4000 bytes to Boundary (3000 passed / 3001 caught) and Hangul boundary (`가` |
| 2026-08-02 | Corrected false positive count when counting words in the title `subgraph` as nodes | `diagram/scripts/check.py`(`count_nodes`)·`check_test.py` | When I opened the diagram to process the "`LLMOps 고성능 서빙 및 추론 최적화` crosses the D2 promotion line with 16 nodes" left over from the previous item, **the actual nodes were There were 7** (`USR`·`G_IN`·`G_OUT`·`RAG`·`VDB`·`VLLM`·`OBS`). 9 out of 16 were **words in the subgraph title without quotes**, such as `subgraph Client Layer`·`subgraph Security & Guardrail Layer` — `Client`·`Layer`·`Security`·`Guardrail`·`Context`·`Serving`·`Engine`·`Observability`·`Evaluation`. `count_nodes` skipped only `style`, `classDef`, etc. and skimmed the `subgraph` line, and `NOT_NODES` filtered out only the word `subgraph` and passed the title. 🔥 **False positives were in a bad direction** — The checker was wrong in recommending an upgrade**, saying "This diagram is past Mermaid, so render it in D2", and if I had followed that advice, I would have moved a 7-node figure into the SVG pipeline and managed the source, render, and embed. Diagrams that followed this skill's rule of surrounding labels with quotation marks did not suffer from this false positive in the first place (the title was recognized as a label and was deleted by `strip_labels`), so only diagrams that did not follow the rule were selected and incorrect advice was received. The `subgraph` line was modified to count only one container id — `subgraph SEC["…"]` with quotes counts `SEC`, and `subgraph Client Layer` without quotes counts `Client`. **Verification**: Two new tests (the five-layer, seven-node diagram is not recommended for promotion, and the form with the id is the same), all existing tests passed. In the actual vault `wiki/`, in the entire mermaid block 23, **1 promotion recommendation → 0, and there are no recommendations remaining. As for the target note itself, following the skill rules, 5 subgraphs were given IDs and titles in quotation marks, and node labels were enclosed in quotation marks (including `VDB[(…)]`). Re-examined the entire vault. There were 0 subgraph titles without quotes. |
| 2026-08-02 | Added a rule so that the `diagram` checker catches wiki links parsed as Mermaid shapes. | `diagram/scripts/check.py`·`check_test.py`·SKILL, Korean skill information | The user posted a note and said, “The link is not connected properly.” All of the target notes existed, and the problem was the grammar surrounding the link. The 3 backtick code spans were self-explanatory, but **the bigger ones were the 8 Mermaid `id[[텍스트]]`** — this is a **subroutine geometry grammar** ([Mermaid flowchart document ](https://mermaid.js.org/syntax/flowchart.html)) and its source is the same letter-by-character as the Obsidian wiki link. So the pasted link is parsed without error, the note title is drawn with a double bounding box, and since Obsidian does not interpret links inside the fence **the diagram shows connections that are not in the vault**. 🔥 **No matter how much you look at the render, it's not obvious** — Because the box shape is exactly as intended, which is why this defect is silent. **No one said this as each of the four checkers was working correctly**: On 7/28, `build_graph` was modified to exclude wiki links within the code span/fence from the graph (at the time, the purpose was to eliminate false positives), so dead links do not appear in `missing_targets`, and `garden`'s orphan decision is supported by frontmatter `related` and passes. The `diagram` checker passed it because it was a grammatically valid shape. **There was not a single check asking if "the intended link was dead".** The problem was how to make the decision deterministic - valid subroutine use and the pasted link have the same source - the method of looking up the vault note list was discarded as it binds the checker to the batch, **separated by quotes**: If `[["텍스트"]]`, it is considered the intended shape, if only `[[텍스트]]`, it is considered a link. This skill already requires quotes around the flowchart labels, so it's not a new burden, and the two repair paths (`["텍스트"]` plain box + body link, `[["텍스트"]]` keep shape) come together in the message. However, it is true that the checker has tightened one point where it was forgiving — the existing test was fixed to **pass** `A[[Some Note]]`, so it was flipped along (the test's original intent of "preventing false positives with bracket rules" was kept in quotation marks). **Verification**: 4 new tests (blocking of original form, confirmation of message, passing two repair paths, no effect on prose wiki link), passing all existing tests, reproducing the form before modification and confirming exit 1. Real vault `wiki/` total mermaid 23 blocks, 0 new false positives. **Limitation** — This happens even when you really intend to use an unquoted subroutine. This is by design and can be resolved by adding quotation marks. **Addition**: While verifying the repair results with a graph, the advice initially written in this document and the Korean guide ("Put the link in the text or `related`") turned out to be incorrect, so we corrected it together — Since `build_graph.py` does not have the string `related` at all and only the main text of the note is read, links that exist only in `related` have a Stop hook in both the graph and `vault-gardening`. It doesn't exist in the reach test either. This means that even though it is meaningful as an Obsidian property, it is not included in the judgment of this repository, links removed from the diagram must be written in the **body**. |
| 2026-08-02 | Moved storage layer of GeekNews daily captures from `wiki/` to `raw/` — so scraper can write there | `examples/feed_scraper/scrape.py`·`scrape_test.py`, `docs/guides/feed-scraper.md` | It came from a vault atomicity check. Of the 305 notes in `wiki/`, **177 were automatically collected and captured**, and 177 out of 185 notes in `status: seed` were there, so all indicators counting atomic notes were swept away by the collection amount. A capture containing several unrelated posts in one day is not an atomic note by definition, and section 2 of `AGENTS.md` already divides `raw/` into an immutable original and `wiki/` into a curation atomic note, so the rules were not changed, but the misaligned arrangement was aligned with the rules. **Just moving the note will bring it back** — As the user pointed out, if the scraper continues to write to `wiki/`, it will be restored in the next run, so `DAILY_DIR` was separated into `raw/geeknews`. The wall index, which is the entrance through which people pass, and 7 topic documents, which are the results of people's selection, were left in `wiki/`. `refresh_month_index` reads the list from `raw/` and writes the index to `wiki/`. **Testing actually caught the bug** — In the past, when writing a daily report in the same place, `wiki/GeekNews/` was created automatically, but when the layers were split, the index write died as `FileNotFoundError` (a flaw that only occurs on new installations, so it would not have been visible in the current vault). `makedirs` was added to index writing. Obsidian unpacks wikilinks by name across the vault, and `vault-gardening` also reads `raw/` file names for the same reason, so 177 `[[2026-08-01]]` in the index are still stuck — **broken link 0** the same before and after the move. **On the same day, a user pushed the boundary once more** — "Wouldn't it be okay to send the geeknews index as raw as well? And what's in geeknews/topic could be put in the geeknews path." That's a correct point. In the first judgment, the wall index was left at `wiki/`, saying it was "the human entrance to capture," but **there is no curation judgment in that file** — it is just a list of links made by going through the folder, so it is an automatic creation like capture. When the standard is changed from "Is it an entrance?" to **"Has human judgment been included?"**, the index goes to `raw/`, and then only the subject document remains in `wiki/GeekNews/`, making one layer of `주제/` meaningless (if you dig a subdirectory in a folder with only one type, the path only becomes longer). `VAULT_DIR` was removed and organized into `DAILY_DIR` (raw, automatically created) and `TOPIC_DIR` (wiki, human-defined classification). Recursion was also rewritten on that basis — "month indexes remain in the wiki" replaced with **"month indexes are also in raw"·"raw output does not touch the wiki"·"topic documents do not have subdirectories"**. The instructions in the creation template were also corrected — the topic document said "The original by date is in `GeekNews/<연월>/`", which is now incorrect (the 7 files that were already created were also corrected). The month index notice **past months are not updated** — `refresh_month_index` is only called for the current month, so the 7 already created were directly unified. At the same time, I changed the three callouts from `> [!NOTE]\n> 본문` to `> [!NOTE] 본문` on a single line — the vault convention is to put the cover and first sentence on the same line, otherwise `unwrap.py` in `note-writer` will catch the scraper output as a violation every time, fix it, and then go back on the next run and only shake the mtime. **Verification**: All items passed the scraper test, three types of layer separation were fixed by regression (capture under `RAW_ROOT/geeknews`, month index and subject document under `WIKI_ROOT`). `garden.py` actual measurement: 305 notes → 128 notes, broken links maintained at 0 |
| 2026-08-01 | Correcting telegram/skill map drift in root README | `README.md`, local `REGISTRY.md` state comparison | In the process of checking the Telegram bot that was reactivated as read-only, only the root function description maintained the old “write notes directly” action, and among the actual 17 skills, `vault-search` was also missing from the skill tree. We adjusted the Telegram description to the actual boundaries of the search, inquiry, Q&A and tool gates, changed the exaggerated expression of “complete security isolation” to a specific blocking range, and added `vault-search` to the skill tree. The bot execution status and `d2` installation status for each machine were not tracked and were only updated locally on `REGISTRY.md`. |
| 2026-08-01 | Add a separate `PreToolUse` egress gate for external notebook runtimes | New `.agents/hooks/study-egress-guard.py` and `study-egress-guard_test.py`, `.claude/settings.json`, `.codex/config.toml`, `.agents/hooks/hooks_test.sh`, `AGENTS.md` and `AGENTS.ko.md` section 5, `study-install` section 6b, [ADR 005](adr/005-egress-guard-for-external-runtimes.md), docs map, root README | A user asked whether the agent could reliably decide when to use an external runtime. Inspection showed that **the boundary existed in documentation but lacked enforcement**: AGENTS section 5 already required separate approval for vault transfers, yet the first Colab MCP session relied only on agent judgment. The existing matcher, `Write\|Edit\|MultiEdit\|NotebookEdit\|Bash`, did not cover `mcp__colab-mcp__*`. **Approval prompts do not close this gap**: they ask whether to run a tool, not whether its payload contains notes. The likely failure is a plausible next step, such as copying note content into a notebook to compare recorded numbers. **The existing `study-tool-guard` was not expanded.** It activates only when `STUDY_SURFACE` identifies a surface without prompts; that rationale concerns where writes land, not what a payload contains. Reusing it would silently broaden its documented premise. Its Telegram surface was also dormant after retirement on 2026-07-31. The new gate blocks vault structure: knowledge-root paths and names, `vault/wiki\|raw\|index.md\|log.md\|hot.md`, simultaneous `verification:` and `checked:` fields, and wikilinks. It does not block public dataset measurements merely because the same numbers occur in notes. For the same reason, `[[1, 2]]` is treated as a nested list literal rather than a wikilink. **Limitations:** this is a targeted check for one known channel, not general DLP; paraphrased and encoded content can pass. Codex `PreToolUse` support was not verified in a live session; only TOML parsing was checked. Claude Code was the verified execution path. All 15 tests passed, covering the actual public WM-811K class-count cell as an allowed regression case and note bodies, paths, wikilinks, nested arguments, and the Antigravity dialect as blocked cases. |
| 2026-08-01 | Set default active skill of Telegram bot to `vault-search` — not forced dictionary lookup, but default skill | `examples/telegram_bot/bot.py`·README, `docs/guides/mobile-telegram-bot.md` | User suggestions (“Wouldn’t it be great to have vault-search run by default when user input comes in?” followed by “It would ask questions based on what’s in the vault”). The direction is right — the input on this surface is almost entirely vault lookups, and rule #1 of `using-study`, which says "look for existing notes before answering," is skipped because it's prose (the same logic that made `study-note-check.py` a hook). **However, “For each input, the bot searches the code and injects the results into the prompt” was not adopted.** This is because embedded search returns results no matter what is entered — In actual measurements, `"고마워"` gave GeekNews “It’s okay to fall behind, thank you!” with **0.453**, and `"아까 그거 다시 설명해줘"` had “Retry-now Autonomous Loop Agent” with **0.535**. Raised (0.61 to 0.62 when truly relevant). The last one is particularly bad, as it involves inserting irrelevant notes into a follow-up question whose answer is already in the conversation history. Moreover, if the excerpt is already in the prompt, it's tempting to quote it before opening the note, but the contract of `vault-search` is that the result is just a pointer, which is the same rule as the knowledge graph. If you leave it as a skill, the model will skip greetings and follow-up questions. **Cost was not an obstacle** — Since it is a local embedding, when there are no external calls and no drift, the query takes **approximately 1.0 seconds**, and the first query immediately after changing 5 notes takes **approximately 43.9 seconds** (including re-embedding), but this value gets smaller the more frequently you run it. The sticking point in the implementation is that **"never picked" and "turned off manually" cannot be left in the same state** — if `reset` is `pop`, the default will be turned back on in the next message, making it look like the turn off button did nothing. Store the empty string as explicit off, and `active_skill_for()` separates the two. **Verification**: Passed 12 state machine tests (default/selection/off, `bot_state.json` stays off in save/restore round trip, clear is excluded from restart restore notification, selected skill is subject to notification) |
| 2026-08-01 | Turn knowledge roots to **read-only** on surfaces without approval prompts — Telegram bots only search, query, and answer questions | `.agents/hooks/study-tool-guard.py`·`study-tool-guard_test.py`, `examples/telegram_bot/bot.py`·README, `AGENTS.md`·`AGENTS.ko.md` Section 5, `docs/guides/mobile-telegram-bot.md`, `study-install` SKILL §5·§6, root README, docs MOC, Skill guide | User request ("Remove all document creation functions and use only search skills, i.e. only question and answer"). This bot retired on 2026-07-31 for exactly that reason — it bypassed `note-writer` and left a note directly in the vault that broke the Frontmatter contract (`f2a3fae4` @39). The response at that time was to add a contract check to the hook, but **well-formatted front matter and a note with value to leave are not the same thing.** Contracts can be viewed mechanically, and I actually looked at them well. What you cannot see is whether the claim has been verified, whether it does not overlap with existing notes, and whether `index`, `log`, and `hot` moved together, and all three are judgments. No permission prompt means **no one to judge**, so we closed the knowledge root on the surface instead of making the test more sophisticated. When `vault` was removed from `writable_roots()`, `wiki/` note, `raw/` capture, navigation file, deletion, and shell redirection were all blocked at one point, and about 90 lines of Frontmatter inspection that became unreachable were deleted (contract enforcement is done through `PostToolUse` hook `study-note-check.py` in the interactive session). remains the same). I left `_workspace/` and the temporary directory open — **because reading is not without side effects** The semantic index of `vault-search` is there and updates itself when the note changes, but if I close it, the search itself stops. On the bot side, the `/skill` button is left with only the query skill (`vault-search`·`study-session`·`vault-gardening`; `recall`·`paper-search` removed — **You can turn it on, but the mode that blocks it in the last step is the worst**) and a prompt notifies each request that it is a read-only surface. The hook alone may seem sufficient, but the hook can only be rejected after the model **decides to call**, and on the phone the round trip only appears as a few minutes long `생각 중...`. **Verification**: Passed 44 hook tests. We explicitly fixed the inverted expectation value — blocks notes that fully satisfy the contract, blocks capture of `raw/`, blocks even past symlinks/relative paths, blocks erasing notes with `delete_file`, and allows writing and vault **reading** of `_workspace` to pass. |
| 2026-08-01 | Codify MCP server installation as profile gate — ban `corporate`, add colab-mcp selection confirmation procedure to `study-install` | `AGENTS.md`·`AGENTS.ko.md` Section 5, `study-install` SKILL §5, Skill guide, `REGISTRY.md` (local record) | Immediately after the user registered the official `googlecolab/colab-mcp` on a personal machine for a GPU lab that exceeded the local 8GB limit, it was requested to "prohibit installation of MCP in the company, and check whether it is installed during study-install." This server is subject to the existing "corporate basic prohibition of selective third-party transmission" clause because the code and data executed by the agent are externally transmitted through Google Runtime, but the form of MCP was not specified in the clause, so there was room for interpretation at each installation time. The standing policy was codified in one line in AGENTS section 5 (prohibition should always be on, so it is not placed in a skill), and the repeating procedures (detection command · `--scope user` registration · OAuth · tool reflects next session · REGISTRY record of decision) were sent to the profile gate section of study-install §5. The reason why the corporate recorded `blocked-by-profile` was to prevent the same question from being asked again in future sessions. No automated testing was added — the main body of the gate is an installation interview action, so it is not subject to mechanization, and detection is a one-line deterministic command written on paper. |
| 2026-08-01 | Add regression test to `unwrap.py` | `note-writer/scripts/unwrap_test.py` | This repository requires executable tests for critical scripts, which were not present in `unwrap.py`. Since this is a script that rewrites notes in place with `--write`, the dangerous failure is not “the folded paragraphs were not merged” but **line breaks were structured but were merged** — the notes that were damaged like that are seemingly fine. Front matter, fence code, tilde fence, table, title, list (bullet/number), separator line, indentation code, footnote definition, as well as two types of hard breaks (two spaces at the end, backslash) and even the prose line immediately before the list were set to "not touched." Basic reporting, application of `--write`, and no change in the 2nd round are also fixed. While writing the test, it was discovered that `unwrap()` returns a `(텍스트, 건수)` tuple, so the test was correct (the script operates normally). |
| 2026-08-01 | Establishment of new local embedding server and `vault-search` skill, addition of installer to `study-install` | New `vault-search` SKILL·`scripts/semantic.py`·`semantic_test.py`, new `study-install/scripts/install-embedding.sh`·SKILL, `note-writer`·`using-study`·`ingest` SKILL wiring, root README·Korean skill information | Keyword search can be found only if you already guess the words written in the note. This is where two of the vault's failures in repetition emerge — the same concept is used twice in different words, and the answer is answered using model memory even though there are existing notes. **Model selection determined the success or failure of this tool.** When measured with 8 Korean queries for which the answer notes are known, the English-centric `nomic-embed-text` ranked in the top 10 with **2/8·MRR 0.067**, and GeekNews' "How to **prevent** customer churn" in "How to automatically block the results produced by agents before deployment" was placed above the answer notes (not meaning). stuck at `막다`). In the same set, multilingual `bge-m3` is **8/8·MRR 0.573**. So the default is multilingual, and the script asks for and selects the model installed on the server instead of hard coding (the installer selects according to the specifications, so the fixed default value is a model that has not been received). If there is an index, that model takes precedence — the vectors of the two models are not comparable, so changing them silently is not an improvement, but rather a deterioration. **We did not create a structure for humans to remember rebuilds**: Rebuilds that require triggers do not work in the end, and old indexes, while giving plausible results, fail at their worst, being read as "not in the vault" except for the note just written. Only notes where `query` has been changed to the content hash are first re-embedded (1 to 2 seconds after the writing session, 0 if no change). **Does not go out** — Since there are turnover data and private notes in the vault, endpoints that are not loopback are rejected by the script rather than configured, and the index is placed at `_workspace/` outside the synchronized knowledge root. The rule that the result is a derivative, navigation, and not evidence was specified in three places: skill, script, and output phrases (same rule as the knowledge graph). **Verification**: 22 tests (loopback rejection, whether chunking carries titles together, overlap, normalization, drift determination without server, whether index is outside the vault), actual vault 286 notes 3,866 chunk index |
| 2026-08-01 | Confirmed to launch the embedding server as a dedicated LaunchAgent rather than `brew services` | `study-install/scripts/install-embedding.sh`, `REGISTRY.md` (installation location) | Homebrew's llama service plist includes `OLLAMA_FLASH_ATTENTION=1`·`OLLAMA_KV_CACHE_TYPE=q8_0`, which is delivered to llama-server as `--flash-attn on --cache-type-k q8_0 --cache-type-v q8_0`. Both are options to reduce the KV cache of the generated model, but the embedding model is an encoder, so there is no such cache. The result was a quiet half-failure — the server answered `/api/version`, but stopped before `ggml_metal_init`, and the model never loaded. `ollama serve`, which was launched by hand, uploaded the same model to `--flash-attn auto` without cache-type within 1 second. **This symptom was misdiagnosed as a hardware limitation, and based on that judgment, the function was removed once and then restored** — One line of `inference compute id=cpu` used as evidence was the product of a separate GPU pre-search with the 30-second watchdog expired, and under the same log, there was a record of the entire layer being uploaded to the GPU as `MTL0 (Apple M1)`. “Free 1.6 GiB” is the same CPU path figure, and the Metal device reported 5460 MiB. So the installer uses its own LaunchAgent that doesn't set any tuning variables, and **verifies that the actual embedding is returned, not if the port is open** — this flaw passed all port checks. Homebrew plist is overwritten during upgrade, so there is no option to modify it in the first place. |
| 2026-07-31 | Added rule to prevent hard line breaks in note body and `unwrap.py` check, batch organized 98 vault files | `note-writer` SKILL·`references/note-schema.md`·New `scripts/unwrap.py`, AGENTS·AGENTS.ko | User pointed out. In the meantime, I've been writing paragraphs by folding them around 80 columns, but in Markdown, line breaks inside paragraphs are rendered as spaces, so **it makes no difference to the reader, and only the writing side pays the price** — If you change one word, the entire paragraph is refolded, the diff is covered with refold noise, so you can't see the actual change, and the Obsidian editor refolds to the window width anyway, so the file and the screen are misaligned. Since the rules cannot be followed if they are written down, the judgment was transferred to a script. The difficult part was **what kind of line break structure** - Table, code block, front matter, and title were self-explanatory, but three things were a trap: ① Short labels like `코드:`/`결과:`/`데이터:` and lines that start with a colon may look like paragraphs, but they are actually lists and should not be combined. ② If the entire line is in bold text, it is actually a subheading, so the previous line should not swallow it, but the first implementation mistook even ordinary paragraphs such as `**a**이므로 **b**` as subheadings and missed some of the 187 places — it was fixed to be viewed as a subheading only when there is no `**` inside. ③ Continuous lines of list items must be included within the item, but the first version missed it, so only 1,575 positions were processed, and after reinforcement, the number increased to 1,910. **Verification**: Before application, all 312 markdowns were backed up to Scratchpad, and the before and after were compared with a string with quotation marks (`>`) and all spaces removed, and **0 content inconsistencies**, and **0 changes in the number of table rows and code fences were confirmed. The application of the first version was restored from a backup and then reapplied as an improved version at once. |
| 2026-07-31 | New `ingest` batch mode for bulk documentation — progress is calculated from citations in notes rather than checkboxes | `ingest/scripts/queue.py`·`queue_test.py`·SKILL, Korean skill information | This pattern was discovered by examining document work history with AgentsView. `9d0d45ea` (3 user turns, output 301k, peak 725k) and `2324131b` (4 users, 467k, 708k) are sessions in which `/goal` documents the entire compressed folder in one line, and in both cases, the last user message was an afterthought (`9d0d45ea` @278 "Remove broken links"). The cause was **the task list was only in the conversation context**. When the list of files was pushed out of sight after dozens of notes, the model **started to write the source path as memory**, and 6 citations with the delimiter of `source-files/` incorrectly written as `__` came in (the actual files were `_`, `slides-text/`·`notebooks/` were really `__`, so they were lumped together) Substitution was also dangerous). Checklist files inherit the same weakness — it's also something to remember to check. So **don't save anything**: whether a capture file is "complete" is calculated based on which note actually wrote the path to `sources:`. Even if the session is interrupted, it resumes from the notes, and old checklists can't lie. Since the waiting list is a **real path to copy**, there is no reason to type it from memory. Here, a flaw in macOS was revealed — the file system returns Korean characters as NFD and notes are saved as NFC, so when comparing the paths as strings, `효율화` is different. All 45 captures were reported as "unquoted" (actually 6 were cited), and `Path.exists()` is hidden by the OS and is only revealed when string comparisons are made. NFC normalized on both sides with `key()`. **Verification**: 13 tests, confirming that reverting NFC modifications to their original locations will cause the tests to fail. `source-files` 6/45 in actual vault, lecture capture overall 54/101 |
| 2026-07-31 | Stop hook reachability judgment unified into one `garden.unreachable_notes` set | `.agents/hooks/study-wrapup.sh`·New `study-note-record.py`·`hooks_test.sh`, `vault-gardening/scripts/garden.py` | On 2026-07-26, `garden.py`'s orphan decision was corrected to "What points to me", but the **Stop hook was left the same**. The hook is passed as `grep -qF "$title" index.md`, and there are two reasons why there is a title in index.md — it is either categorized by the topic hub (persistent), or is in "recently updated" (limit of 5 lines, pushed out after the 6th). In other words, the quietly orphaned structure remained for five days, equal to the time gap between the moment the hook passed through and the time it was pushed out. That's exactly how the A100 note went out (`f2a3fae4` @395 "Why is there no entry point"). The real flaw is that it lumps recordability and reachability into one check, so I split the questions into two. The log check retains the existing behavior of accepting either index or log — tightening this false positive profile increases alerts even if the rule is correct, and this hook has already been ignored twice due to false positives. Reachability calls `garden.unreachable_notes` (a recursive flaw in this repository is that if you write the definition in two it splits). **The scope is limited to the notes written in this session** — If you look at the entire vault, one existing fan blocks each turn and teaches you to ignore the hook. Graphs are created only when something is written, and it takes about 1 second for 286 notes. **Verification**: Hook test passed, confirming that new regression tests (notes only in index plane list) quietly pass on `study-wrapup.sh` before modification. |
| 2026-07-31 | A new `PostToolUse` hook is created to inspect notes the moment they are saved, and inspection definitions are shared in `garden.py`. | New `.agents/hooks/study-note-check.py`·`study-note-check_test.py`, `vault-gardening/scripts/garden.py` (`note_contract_findings`·`source_anchor_findings` separated), `.claude/settings.json`, `.codex/config.toml`, root README | The checker itself was good — the Mermaid label check matched the Obsidian bundled renderer against 235 actual labels, yielding zero false positives and zero false positives. What wasn't there was **caller**. All call points were SKILL.md prose ("run before finishing") and prose compliance was probabilistic. AgentsView investigation found that all three sessions start the same — `dbeebb2a` @0 "Error parsing Mermaid diagram! often occurs", `5773c188` @0 "unsupported markdown list occurs", `b43fb78d` @0 "Inline formatting blind spots over line breaks". All three **were discovered by the user in Obsidian** after they had already been written and synchronized to the vault**, and it took one session to track down the cause (total of about 560,000 output tokens). Hooks remove selection: we check it because we wrote it. Writers who do not go through skills at all are also covered, and skill bypass was precisely the reason why a retired Telegram bot created a note that broke the Frontmatter contract (`f2a3fae4` @39). I narrowed it down to **the one file I just wrote and the questions that can be answered with just that file** — Reachability/backlinks require a graph, and creating a graph for each edit in a cloud-synchronized vault makes writing impossible. The contract check and source anchor check are taken out as functions in `garden.py`, and both callers use the same definition. **Side Achievements**: This inspection cleaned up 19 broken source anchors in the actual vault — 6 path typos corrected, 13 citations to `_workspace/pt-experiments/`, whose directory is git-ignored and outside the knowledge root, so they are not preserved, so 21 were updated after a snapshot with `raw/experiments/pt-2026-07/`. `hot.md` was also organized from 3463 to 1975 bytes and corrected the old technology of “sustainability report PDF not secured” (already in `raw/hynix-pt-2026/`). Gardening pointed out: 23 cases → 1 case. **Verification: Passed 20 new tests (false positive direction also fixed — created list, files outside the vault, non-writing tools, index.md contract waiver), passed all 6 existing tests |
| 2026-07-31 | Added a check for "Is the link sent by this session being interpreted?" to the Stop hook, and removed false positives for the `raw/` capture link on that premise. | `.agents/hooks/study-note-record.py`·`hooks_test.sh`, `knowledge-graph/scripts/build_graph.py`·`build_graph_test.py`·SKILL, `vault-gardening/scripts/garden.py`·SKILL, `AGENTS.md` (Section 8)·`AGENTS.ko.md`, Korean skill information | This was revealed when a user asked, “Wouldn’t it crash if I work on a document in different sessions?” — At the time of the question, **another session was actually running**, and while using `P&T 28`, it was **linking to `P&T 29`, which does not exist yet**. The Stop hook asks only two of three questions: Was the note written and what points to this note? No one asked **whether what this note refers to** exists. Wikilinks are references **between** files, so they cannot in principle be seen by file-level inspection — even if there is no target, the note is saved as perfectly normal Markdown and passes the history check, and the defect remains quietly until the next time someone follows the link. This is the same reason why it cannot be resolved by merging the work tree and Git (each file is well-formed, so there is no conflict). **When I opened `missing_targets` to paste, 2 out of 3 were false positives** — `build_graph` only scans `wiki/`, but since Obsidian interprets wikilinks across the vault, there is a normal link in `raw/hynix-pt-2026/` pointing to two **real** captures (`2026-07-31-미확보자료-5종-종결판정`·`2026-07-31-지속가능경영보고서2026-1차출처-발췌`) Caught broken. If I had just hooked it, it would have **blocked every turn with a normal note**, and that is a failure mode (training it to ignore warnings) that this hook has already gone through twice and had to recover from. So I put `--raw` first — it reads only the name**, creates no nodes or edges, and does not parse the body. Since `garden.build_graph()` automatically passes over `raw/` if it exists, a "broken link" definition with the same report and hook is used (a repeating flaw in this repository is that if the definition is written in two sets, it splits). Like the previous two checks, the scope only looks at links made by notes written in this session** — if you block existing broken links as well, each turn won't end with someone else's debt, and that's exactly what causes the warning to be ignored. The existing debt belongs to `vault-gardening`. **Verification**: In the actual vault, 286 notes and 317 links, 3 cases of `missing_targets` → 1 case, the remaining 1 case is real (`P&T 22` → non-existent `P&T 29`). Execute the hook and confirm that unregistered notes and broken links are correctly pointed out. Added tests - 4 types of graphs (3 reports if raw is not available, 1 if raw is provided, capture does not become a node/edge, not an error even if there is no raw path), 3 types of hooks (broken links in registered/reachable notes are blocked and pointed to both ends, resolved by fixing the link, `raw/` capture link passes). **Limitations** — The hook uses only the first line of discovery, so if unregistered, unreachable, or broken links overlap, they are revealed one by one. And this check only checks whether the link is **interpreted** and does not determine whether the link is **appropriate**. |
| 2026-07-28 | Fixed a blind spot in which two checkers could not see inline formatting that spanned line breaks — Wikilink false positives within code spans, backtick and link false positives across `<br/>` | `knowledge-graph/scripts/build_graph.py`·`build_graph_test.py`·SKILL, `diagram/scripts/check.py`·`check_test.py`·SKILL, `vault-gardening/scripts/garden.py`·`garden_test.py`·SKILL, Korean skill information | The cause is the same in both checkers — **markdown is read one line (or one section) at a time, but inline formatting does not end at a line.** ① `INLINE_CODE` of `build_graph` is `` (`+)[^\n]*?\1 ``, so only the pair within one line is deleted. A CommonMark code span is closed by backticks of the same length **anywhere**, and the line endings between them are blank, so `` `rg --files\n[[Ghost]]` `` is the entire code. If you read it line by line, you will never see the closing backtick, and the note will be rendered as code just fine, but only the graph will report a "link without target" (reproduced: 1 case `missing_targets` to `Ghost`). I erased it from the entire text to preserve the number of lines, and because blank lines end blocks, I blocked them from crossing paragraphs with `(?!\n[ \t]*\n)` — without this, two solitary backticks in the prose would swallow the link in between them whole (fixed as a test). ② The label check of `diagram` runs 7 rules **per section** cut by `<br/>`. The list, title, quote, and separator lines are block rules that look at the beginning of the line, so **backtick pairs and `[텍스트](주소)` are inline**, and Mermaid passes the entire label to a single lexer. I verified this by directly lexing it with local `marked` 13.0.3 using mermaid's `<br/>` → line break preprocessing — `` run `rg<br/>--files` here `` produces a **codespan token**, and `see [the doc<br/>page](http://x) now` produces a **link token**. In other words, labels that appear to be plain text in each section are actually replaced with `Unsupported markdown`. The rules are divided into block and inline, and inline applies to all labels with `<br/>` erased. **Bold and italics were left as is on purpose** — In the same experiment, `**굵게<br/>이어짐**` appears as strong and `*기울임…*` appears as em, and both are supported types, so they are rendered normally. Measures, not rules, set boundaries. Two defects of the same series that were revealed together were also fixed — `garden.py`'s index link budget scans the original text with regular expressions **without any distinction between fences and code spans** (if reproduced with an index containing grammar examples, it counts as 23), and now borrows Graph Builder's text reading as is (if written in two sets, the definitions are split and the budget reports links that are not in the graph). The embedding check for `check.py` also counted the inside of the fence and pointed out `![[…svg]]`, which was written as an example, as an “absent asset,” so it went through masking to preserve line numbers. **Verification**: 231 actual vault notes, 0 cases, 25 mermaid blocks normal (0 new false positives), all 11 Python tests passed, 3 new tests confirmed with stash that each failed in the script before modification (checking regression detection). Limitations — `marked` was running 13.0.3 locally and was not running the Obsidian bundled version directly. The code span not closing at the end of the line is a CommonMark rule and has nothing to do with the version, but bundle oracle matching was not performed. |
| 2026-07-26 | Define reachability by incoming references and fix escaped wikilinks in tables | `vault-gardening` SKILL, `scripts/garden.py`, `garden_test.py`, `knowledge-graph/scripts/build_graph.py`, `build_graph_test.py`, skill guide | A user asked why the A100 note had no entry point. It had **zero incoming links**, yet all four checks passed because they accepted an outgoing link or a mention of its name. `build_graph` defined orphans as `not links and not backlinks`, so six outgoing links excluded the note (`build_graph.py:472`); `garden.py` inherited that result. The Stop hook accepted its name in the index's five-entry recent-updates list, so the note could silently become unreachable when that entry aged out. Gardening now reports notes without either `backlinks` or `categorized_under`. Inspection confirmed that the latter counts the five topic hubs, not recent updates, so hubs remain reachable while cases like A100 are reported. The same change fixed `normalized_target` splitting on `raw.split("\|")`: a table's escaped alias separator `\|` left a trailing backslash in `vLLM and PagedAttention`, falsely reporting two valid links as broken and omitting two real edges. Rechecking the vault reduced broken-link reports from two to zero and found no unreachable notes. Five tests cover outgoing-only notes, backlink-only reachability, category-only reachability, escaped-pipe links becoming edges, and their exclusion from missing-target reports. |
| 2026-07-26 | `d2` included in Phase 2 installation target | `study-install` SKILL·`scripts/install-phase2-tools.sh`·`install-phase2-tools_test.sh` | The `diagram` skill instructs to upgrade to D2 when the number of nodes exceeds 15, but `d2` was not installed and was only listed as "optional later diagram rendering" in the SKILL document. When I tried to move the A100 platform note (30 nodes), it was found to be missing, and I had to ask for installation on the spot — the rule was telling me to escalate, but not giving me the tool to escalate. Since it is a single binary like `yt-dlp`·`ffmpeg` and shares the same package-manager path, I put it in a loop and expanded the variable name from `media_tool` to `brew_tool`. `--check` also reports d2 — to ensure that renderer members do not hide until the diagram build fails. Added 2 types of tests (in case of installation failure, d2 is individually pointed out, d2 is included in `--check` output) |
| 2026-07-26 | Introducing PreToolUse gates on surfaces without approval prompts — blocking out-of-vault writes, home sweeps, and note contract violations | New `.agents/hooks/study-tool-guard.py`·`study-tool-guard_test.py`, `.agents/hooks/hooks_test.sh`, `.claude/settings.json`, `examples/telegram_bot/bot.py`·`bot_test.py`, `study-install` SKILL, `AGENTS.md` (Section 5)·`AGENTS.ko.md`, Telegram Guide·README | When I asked the Telegram bot to document this, macOS displayed “python3.11 trying to access reminders.” The reason is two-fold — 1) the bot launched the CLI as `--dangerously-skip-permissions`, so there was **no gate blocking the tool call**, and 2) the model ran `find ~ -maxdepth 3` to find the vault whose name `REGISTRY.md` had already written down, and entered the same TCC-protected folder as `~/Library/Reminders`. The reason the dialog pointed to `python3.11` rather than CLI is because TCC attributes it to the **responsible process** (the parent that launched the child, i.e., the bot). Through the same hole, notes were being used directly on `wiki/` without a contract. So I created a gate that only wakes up on surfaces without a prompt — I limit writing to the vault·`_workspace/`·temporary directories (deliberately excluding the storage itself: changing trace files is a metaskill's job), block home sweeps and protected paths/private app AppleScripts, and check the `wiki/` new note's frontmatter 8 fields for matching status·verification·date format·H1 if they are out of line. Send it back to note-writer. **During the design process, one premise was revealed to be wrong** — `agy` called by the bot was not Claude Code but Antigravity CLI (1.1.7), so the `.claude/settings.json` hook was not applied in the first place. The actual contract was obtained by observing the session: the payload is `toolCall.name`+PascalCase arguments (`write_to_file` is `TargetFile`/`CodeContent`, `run_command` is `CommandLine`/`Cwd`), the response is `{"decision":"deny"}` and Claude's `block` is an unknown value. The registration location was also different — the project `.agents/hooks.json` used as an example in the document was **not loaded** (the only file read by the log was `~/.gemini/config/hooks.json`) and is registered only in the global file. We also measured that no-output is “not involved” on both sides (writing passes as is with no-output hook on). Confirmed with 4 types of live — blocking writes outside the vault, blocking `find ~`, blocking notes that violate the contract (the reason points to the note-writer), and non-interference with unmarked sessions. Limitations are noted: the shell cannot be completely locked into patterns (`python3 -c "open(...)"`), so the real line of defense is writing tool path checking. 49 types of tests |
| 2026-07-26 | Added Mermaid label markdown render failure and `\n` invalid line break to the check list. | `diagram` SKILL·`scripts/check.py`·`check_test.py`, `note-writer` SKILL, Skill guide, 8 vault notes | The diagram of `파운데이션 모델…6대 발전 패러다임` had `Unsupported markdown: list` in place of the label. The label check I put in last July couldn't see this because it only looks at the **parser** — this diagram is parsed normally and the failure occurs at the render stage. The cause was directly confirmed in the mermaid code of the Obsidian bundle (`obsidian.asar`): labels are passed to the `marked` lexer regardless of whether they are quoted, the only supported tokens are paragraph·text·strong·em·html·escape, and the rest are replaced with the `Unsupported markdown: <type>` string. In other words, **quotes only fix the parsing, not the render**. `1. 루프…` became an ordered list and disappeared entirely. The second defect revealed together is `\n` — there is no `\n` → line break conversion in the markdown label path (the conversion only exists in the non-markdown path `nonMarkdownToHTML`), so it appears as two characters: a backslash and `n`. The checker determines the label text to be a markdown symbol (order/bullet list, title, quote, separator, backtick, link), separates `\n`, and also checks the section after `<br/>` (the HTML label path covers only the beginning, and the SVG label path covers both because the block starts every `<br/>`). **Verification**: 235 actual labels from the vault were rendered and compared with Oracle, which copied the bundle code as is. — The checker detected 40 broken labels (List 26, `\n` 14) as exactly 40, with 0 false positives and 0 false negatives. After fixing, all 235 labels were normal. The number was changed to `①` — `1)` is also a list marker, so if you change the punctuation mark, it will be broken, and for the `1\.` escape, the number will disappear because the SVG label path throws the escape token. 8 impact notes (6 in list, 2 in `\n`, both in `파운데이션…`) |
| 2026-07-26 | Move source selection criteria from number of cases to concept density and turn ROS Discourse off by default | `examples/feed_scraper/scrape.py`, vault notes/collection documents | In the first sample where 61 collected cases were actually reviewed, **the number and density moved in the opposite direction.** ROS Discourse accounted for half (30 cases), and there were 0 concept notes - job openings, meetups, greetings, and individual projects were mixed - while Hugging Face, which received only 3 cases after filtering the title, all 3 cases were valuable. So, I removed ROS Discourse from `enabled` and deleted the collected documents. **I did not delete it from the catalog**: As soon as you start using ROS directly, the actual thread becomes the most valuable, so I left the evidence in the comments so that I could retrieve it. Together, we verified the recall of the HF filter — with 30 robot domain words (`vla`, `grasp`, `mujoco`, `isaac`, `locomotion`, `tactile`, `urdf`, `slam`, etc.) By re-matching the 831 articles, it was confirmed that **there are currently 0 robot articles missing 9 words**, and all 12 candidates were LLM articles (8 articles `rl `, 3 articles `reinforcement learning`, and 1 article `depth`). I left it as is because widening it means only increasing contamination, not recall. The limit of one sample for both measurements is specified in the verification table of the note. |
| 2026-07-26 | Extended stop hook's automatic creation exclusion from tag to note `type` | `.agents/hooks/study-wrapup.sh`·`hooks_test.sh` | A monthly index re-creation put in on the same day was immediately hooked — `2026.7 인덱스` was blocked as a "note not recorded in index.md", which is a derived list that the scraper recreates on each run, so the entry point is not something to name by item. The exclusion rule only looked at the tags `feed`·`daily-scrap`, so the month indexes (`GeekNews`, `Archive`) and subject documents (`geeknews`, `curation`) were missing. Looking at the actual distribution, the boundary was already at `type` — the automatic creation is `index` (monthly archive)·`moc` (queue by topic/source), and the handwritten map is `topic`·`source`. So, except for `index` and `moc`, handwritten maps are still checked. The amount collected by date is `article`, so the existing tag rules apply. Added 3 types of tests (excluding index, excluding moc, handwritten hubs are blocked and named if not registered) |
| 2026-07-26 | Prevents the issue where collected date files were missing from the archive index and became orphans. | `examples/feed_scraper/scrape.py`·`scrape_test.py` | vault-gardening reported `2026-07-25` as an orphan note, but frontmatter's `related` was normal. When I opened the graph, it was inbound 0 and outbound 0 — **`build_graph` does not count frontmatter `related` as an edge, but only the main text wiki link.** Other dates were linked by the month index, but only 07-25 was not in the index. After comparing 6 months' worth, there was only one day missing, and the past months were complete as an index was created during the batch cleanup. In other words, **the scraper only used date files and did not touch the index, creating new orphans every day**. `refresh_month_index()` scans that month's folders and recreates the list — the reason it's a regeneration, not a line append, is because the index is a derivative of the file that's been reproduced, so any omissions from the past are filled in as well. The number of notices is also consistent with the actual number. If the content is the same, it is not used and mtime is not touched for each execution (fixed to 6 types of tests) |
| 2026-07-26 | Change the knowledge log to the most recent and correct the related rule text together. | `AGENTS.md` (Verses 2, 3, 5), `AGENTS.ko.md`, `note-writer` SKILL, `using-study` SKILL, vault `log.md` | The logs were piled up starting from the oldest, so I had to go through 27 logs to see the most recent work. When setting up the context for starting a session, the most common thing to look at is the recent records, but the reading direction was reversed. Only the data rows are reversed (sorted and compared to check for zero content loss or change), and new items are placed in the first row of the table. **The expression "append-only" was found in four places in the rule, so we fixed it** — If left as is, it contradicts the new contract, and the next session will follow the document and append it to the bottom. However, the only thing that has changed is the reading direction, and the contract of "not modifying or deleting items already written" remains the same, and this is stated both in the rules and in the `log.md` preface. The Stop hook searches the date of `log.md` to `sort` to find the maximum value, so it operates regardless of the order (re-verification completed) |
| 2026-07-26 | Block API tokens left in Telegram bot logs | `examples/telegram_bot/bot.py`, new `examples/telegram_bot/bot_test.py` | The Telegram API puts the token in the URL path (`/bot<TOKEN>/getUpdates`) and httpx leaves the request URL as INFO. Because polling occurred every 10 seconds, the log was being filled with copies of the token — in the actual measurements, there was a token at line **5,783** of the existing `bot.log`. Although it has never been committed in `.gitignore`, it goes out the moment you paste or share the log in a problem situation. I blocked it in two — raised the httpx logger to WARNING to remove the normal request log, and added a masking filter to the root **handler** in case exception messages or URLs leak back to other libraries (the reason it is a handler, not a logger, is because if you attach it to the logger, only the records that go through that logger will be filtered out). 5,783 lines of the existing log were masked with regular expression substitution — the `bot<숫자>:<영숫자>` pattern was used so that the token value was not known. After restarting, 0 httpx records were confirmed for 42 seconds (4 polling times). 9 types of tests added: httpx level, message/format argument masking, non-string argument preservation, handler passing output, 3 types of root search |
| 2026-07-26 | Removed hardcoding of installation path remaining in trace file. | `examples/telegram_bot/bot.py`, `docs/guides/mobile-telegram-bot.md` | `AGENTS.md` §1 says not to place the installation path in the trace file, but the Telegram bot included `STUDY_ROOT` as the default and `/Users/nohdol/nohdol-study`, and the guide diagram also had the same path. On the other computer where the repository was cloned, an environment variable had to be provided to run it. I changed it to the same method I used in the feed scraper — it goes up and looks for directories with `vault` symlinks, so it works the same whether I'm at `examples/` or `_workspace/` (check in all three places). If it can't find root, instead of quietly launching the CLI with an empty `cwd`, it stops at startup and tells you the reason — the failure appears only as a symptom of "the bot doesn't respond," making it difficult to find the cause. Running bots run with a copy of `_workspace/` and are therefore unaffected by this change. |
| 2026-07-26 | Added title filter to feed scraper, expanded robot sources to 6 types, blocked raw HTML tags in link text | `examples/feed_scraper/scrape.py`·README·`sources.local.example.toml`, `docs/guides/feed-scraper.md` | We fixed the three together. ① **Broken rendering** — ROS Discourse's `What should go in the <license> tag?` was saved as is and the renderer read it as an HTML start tag. `defuse_html()` was not applied to the title of `feed` because it was for body text only, and because backticks overlapped within the link text, `clean_title()` returned `<` to an entity (`&lt;license>` is still visible on the screen). The `<script>` series is also neutralized. ② **Coverage gap** — The existing three (IEEE·The Robot Report·ROS Discourse) covered editorial news, industry, and ROS practice, but the research and model axes were empty. In the actual measurement, Robohub (0.4 per day), NVIDIA Robotics (0.2 per day), and Hugging Face (robot) were added to increase only 0.6 cases per day and fill the axis. arXiv cs.RO confirmed that the API is working, but since it is the 34.9th flight per day, it is a query target rather than a queue, so it was left at `paper-search`, and Figure·Unitree·Boston Dynamics recorded that it was structurally impossible because all three were RSS 404. ③ **`title_filter` Option** — In the HF blog, only 23 (2.8%) of the 831 articles are related to robots, so if you enter them as is, the list will be covered by LLM and diffusion articles. By adding a filter to check only the title, 19 cases were excluded and 3 cases were collected. The reason for not reading the main text is the same as the GeekNews keywords — a lot of articles about the topic are posted, and the article you are looking for has that word in the title. |
| 2026-07-26 | Change the stop hook's completion judgment from modification time to file content. | `.agents/hooks/study-wrapup.sh`, `.agents/hooks/hooks_test.sh` | The judgment comparing only mtime produced two false positives and taught the user to ignore the notification. ① If you save a note a few seconds later than the index update, it was caught even though it was already a registered note — in this session, it was actually triggered twice with a 34-second difference, and since the vault is Google Drive, the sync client even rewrote the mtime, so the same file appeared and disappeared every time it was scanned (a situation that AGENTS.md §5 warns about). ② The feed scraper rewrites the collected documents every morning, so the automatic execution day was always caught. The decision was divided into three — notes are looked at **whether the title is in `index.md` or `log.md`** (mtime is left only to narrow the scope of the scan to the most recent), automatically collected documents with `feed`·`daily-scrap` in frontmatter `tags` are excluded as they are not knowledge, and those that do not contain a list of notes are excluded. `hot.md` determines **whether its `updated:` is earlier than the latest date of `log.md`**. Since all three read the contents of the file, the sync does not change even if the timestamp changes. The blocking message was also changed to indicate which notes were missing and by which date they were behind. Added 7 types of tests (passing registered notes, passing just by entering the log, blocking unregistered notes and specifying names, excluding feed documents, blocking hot date lag, resolving by simply updating the date, distinguishing absence of record files) |
| 2026-07-26 | Generalizes the GeekNews-specific scraper into a multi-source feed scraper and moves references to the tracking area. | New `examples/feed_scraper/`, `docs/guides/feed-scraper.md`, `study-install` SKILL, root README, Skill guide, `docs/README.md`, `.gitignore`, `~/Library/LaunchAgents/com.user.study.feedscraper.plist` | The scraper in operation was only in the untracked `_workspace/geeknews_scraper/`, so three things were broken. ① There is no code on the other computer where the repository was cloned. ② The old version (158 lines) remaining in `vault/scripts/` coexists with the operating version (653 lines), so while looking for `clean_title()`·`defuse_html()` recorded by `log.md` in this session, I opened the older version first — I couldn't tell which file was running. ③ The vault absolute path and source list were embedded in the code, so moving it to another machine required modifying the code, and that modification mixed personal choices into the trace file. So, we divided it into **tracking the engine and catalog** (`examples/feed_scraper/scrape.py`), and **non-tracking which source to turn on** (`sources.local.toml`). The vault path goes up and leads to the `vault` symlink, so there is no installation path in the code. There are two pipelines — `feed` stacks only titles and links, resulting in 0 API calls (regardless of external limits even if the source is increased), and `geeknews` maintains the existing score gate, summary, and classification. IEEE Spectrum Robotics·The Robot Report·ROS Discourse were added to the catalog along with actual measurements, and the launchd task was changed from `com.user.geeknews.scraper` to `com.user.study.feedscraper`. Execution verification: In simultaneous execution of 4 sources, GeekNews normal, IEEE overlap 0, The Robot Report 15 cases, ROS Discourse 30 cases |
| 2026-07-26 | Add progress display, cancellation, timeout, and status persistence to Telegram bot | `examples/telegram_bot/bot.py`, `docs/guides/mobile-telegram-bot.md`, `.gitignore` | According to the log, there were 0 crashes in 21 runs, but there were three paths that got stuck on mobile. ① The progress message was static, so it was hard to tell whether a few minutes of work was dead or running — it edited the elapsed time every 5 seconds (`editMessageText` is silent, so there are no notifications) and the typing display was updated every 4 seconds. ② `process.communicate()` had no timeout and no cancellation means, so if the CLI stopped, the phone could not be used. — Insert `STUDY_RUN_TIMEOUT` (default 1200 seconds) and `[⏹ 취소]` buttons and `/cancel` and return partial output together. ③ Since the model, strength, and skill are only in memory, they quietly disappeared when launchd `KeepAlive` was restarted, and the user continued to answer questions without knowing that `study-session` was released — it is made persistent as `bot_state.json` and the restoration is notified (the conversation history itself was already preserved as `--continue` in CLI, so what was lost was the skill prefix injection only). Simultaneous execution is not blocked at the user's discretion, but only a notice is added that session history may be mixed. The status file contains the chat ID, so add it to `.gitignore` |
| 2026-07-26 | Correction to description in mobile guide that lists video length as a risk factor | `docs/guides/mobile-telegram-bot.md` | “One link triggers a long analysis,” he wrote, presenting playback time as a cost risk, but in a 2-pass design, the cost is proportional to the amount of transcript and the number of timestamps selected, not length. In this repository, the actual measured values ​​of the 102-minute lecture (subtitles only 2.3MB, transcript 27k tokens, 12 frames 248KB) were entered as evidence and replaced with three actual constraints (Whisper fallback for videos without subtitles, mobile waiting time, and unintentionally pasted links). Warning of non-existent dangers hides real dangers. |
| 2026-07-26 | Correction of `study-video` description in mobile guide | `docs/guides/mobile-telegram-bot.md` | In the previous commit, `study-video` was written as "explicit requests only", creating a misunderstanding as if the user had to say the skill name or "study this video." What `explicitly selected` in the skill description defines is not a skill but a **video**, and if you paste the URL, it is automatically routed like other skills (it actually works that way in this repository session). What the safety gate actually enforces is `--no-whisper` Always Apply and Timestamp Selection Frame, so I changed it to those two and added a warning that a single link can trigger a long analysis on mobile. |
| 2026-07-25 | Reduce Telegram bot `/skill` menu to 4 types that require explicit conversion and remove `/understand` command | `examples/telegram_bot/bot.py`·README, `docs/guides/mobile-telegram-bot.md` | The menu did not reflect the skill routing hierarchy — `note-writer`·`knowledge-graph` is automatically selected as a request text, but when fixed with a button, the user nails the skill before the router and ends up stuck with the wrong skill. Conversation was reorganized into four types, `study-session`, `recall`, `paper-search`, and `vault-gardening`, leaving only those that wanted to remain in that mode, and `/understand` (5 mode buttons), which was a separate command, was removed as it was redundant because the `understand` skill internally routes nine modes. Mutually exclusive state management (`user_current_understand`), callback branching, `/status` display, and even prompt injection path are deleted together. The reason for not exposing `study-video` and `/watch`, which are for explicit use only, through a button was also specified in the guide. |
| 2026-07-25 | Fix to not count image embeds as knowledge graph edges | `knowledge-graph/scripts/build_graph.py`·`build_graph_test.py` | The `![[assets/....jpg]]` embed was counted as a regular wiki link, and each framed note was reporting an "untargeted link" — even though the file was right next to it. In the actual vault, 16 false positives filled the top of the report, which taught us to ignore the most important sections. In `normalized_target()`, the media extension was returned as an empty string and excluded from both call points (body link and index classification). Note Transclusion (`![[다른 노트]]`) has no extension, so it remains as Edge |
| 2026-07-25 | Transfer of harness rules accumulated in hot.md to each skill | `note-writer/references/note-schema.md`, `study-video` SKILL | `hot.md` is a file defined by AGENTS.md as "a cache, not an authority", but there were only 4 harness rules there, and if it was truncated under the 500 token budget, the rules would also disappear (actually exceeding the budget by 4002 bytes). The rule for prohibiting slashes in the title was moved to the note schema, and the rule for handling rolling duplicates and proper nouns in YouTube's automatic subtitles was moved to the `study-video` procedure. The other two (Mermaid label, index policy) were already in each skill, so they were deleted from the cache. |
| 2026-07-25 | Establishment of a new policy that maintains the index as an entry point and automatic detection of non-interaction | `note-writer` SKILL·New `references/index-policy.md`, `vault-gardening` SKILL·`scripts/garden.py`·`garden_test.py`, root README, Korean skill information | `note-writer`'s index guideline was only "navigational entry", so listing all of the atomic notes was not against the rules, and in fact, the index grew to 59 lines for 24 notes — if the size of the entry point follows the number of notes rather than the number of topics, it becomes a wall that no one reads at a scale of 200. One line of hub notes per topic, no listing of atomic notes, an upper limit of 5 lines of most recent updates (source of truth is `log.md`) were stipulated as reference documents, and a check of `--index-link-budget` (default 15) was added to `garden.py` to determine the number of different wikilink targets, excluding embeds. It is stated on both sides of the skill that prescriptions are always new hubs and do not adjust numbers by deleting items. |
| 2026-07-25 | Added Mermaid label syntax check and connected note-writer diagram gate. | `diagram` SKILL·`scripts/check.py`·`check_test.py`, `note-writer` SKILL, Skill guide, 9 vault notes | Of the 26 Mermaid blocks in the vault, 9 (35%) were failing to be parsed by the actual Mermaid parser, and the existing checker passed them all — it only counts the number of parentheses, so I couldn't see labels that matched like `A[검사 (25ms)]` but were rejected by the parser. Four reasons for failure (parentheses in the node/edge label without quotation marks, parentheses in the title `subgraph`, referring to the title as a node ID, and missing `end`) were detected by reading the label text, and cross-checked with the actual parser to confirm 0 false positives and 0 false negatives. The parentheses of `&`·`#`·`%`·`$`·`/`·`<br>`·Hangul and `sequenceDiagram` are parsed, so they were excluded from the rules. In `note-writer`, there was no inspection execution step at all, so the path was passed through a broken diagram. |
| 2026-07-25 | Telegram Bot Bridge official reference template (`examples/telegram_bot/`) provided | `examples/telegram_bot/`, `docs/guides/mobile-telegram-bot.md` | To solve the inconvenience of losing bot scripts when duplicating Git due to non-tracking directories (`_workspace/`) or having LLMs have to rebuild the code from scratch, we provide verified `bot.py` and `run_bot.sh` as official example templates to build an environment that can be copied and run in 1 second. |
| 2026-07-25 | Configuration and documentation of Telegram bot running automatically when macOS boots (`launchd` LaunchAgent) | `~/Library/LaunchAgents/com.nohdol.telegrambot.plist`, `docs/guides/mobile-telegram-bot.md` | Solve the inconvenience of having to manually enter environment variables and background commands in the terminal every time the computer reboots, and configure `launchd` automatic startup so that the OS immediately restarts (`KeepAlive`) even when unexpectedly terminated (to comply with security rule RULE 5, token settings are safely isolated in the user's personal account path outside the project). |
| 2026-07-25 | Telegram Bot Bridge with MessageEntity-based rendering and local URL purification | `_workspace/telegram_bot/bot.py`, `docs/guides/mobile-telegram-bot.md` | In order to completely solve the problem of backslashes (`\`) or asterisks (`*`) being exposed depending on the client environment when parsing the `MarkdownV2` string, or the API rejecting the `file://` link as an unsupported protocol, the mode is completely reorganized to transmit pure text and style attribute array (`MessageEntity`) separately, and non-web URLs are pre-purified with inline code. Equipped with defense logic |
| 2026-07-25 | Telegram bot bridge markdown rendering enhancement (`telegramify-markdown` applied) | `_workspace/telegram_bot/bot.py`·`run_bot.sh`, `docs/guides/mobile-telegram-bot.md`, `docs/README.md` | To solve the problem of AI CLI responses being output in plain text or parsing errors (`BadRequest`) occurring in `**[Menu]**` due to the strict markdown specifications of the Telegram Bot API, `telegramify-markdown` is installed, which automatically escapes and safely splits standard markdown into `MarkdownV2`, and documents and reflects plain text fallback defense logic in case of exceptions. |
| 2026-07-25 | Understand Anything NPM dependency Automatic installation approval reflected at all times | `study-install`, `install-phase2b-tools.sh`, `understand` SKILL·adapter-contract, Korean skill information | Modify rules and scripts so that `@understand-anything/core` and dashboard NPM dependencies are automatically installed and built when running `study-install` in Node 22+ and pnpm 10+ environments according to the user's decision to approve continuous installation. |
| 2026-07-25 | Phase 1 File-based study harness construction | Repository skeleton, installer, skills, hooks, documentation | Separates knowledge and harness while connecting vault and Claude Code/Codex for each installation location |
| 2026-07-25 | Addition of claim unit evidence verification rules and integration into regular rules | AGENTS, `note-writer` Essential References, Notes Schema | Prevent incorrect information from becoming confirmed knowledge without activating selection skills |
| 2026-07-25 | Phase 2 implementation of ingest·NotebookLM·graph standards | Web capture, paper/video skills, verification export, deterministic graph, installation tool | Convert various materials into learning assets while maintaining original text preservation and verification boundaries |
| 2026-07-25 | Introduction of metaskill rules and reinforcement of skill documents | `metaskill`, root README, Skill guide, existing skills | Ensures harness changes and user routing documents are verified and synchronized together. |
| 2026-07-25 | Complete review of direction and revision of Phase 2b scope | Understand Anything 9 skills, Obsidian 4 skills, NotebookLM CLI, ADR 003, security review, execution spec | Instead of judging other projects, nohdol-study's code, domain, design, and knowledge are evaluated solely for the purpose of learning, and project-local installation boundaries are specified. |
| 2026-07-25 | Additional tool candidates revisited | basic-memory, PaperQA2, Graphiti, Mem0, Cognee, Kuzu, Obsidian REST/MCP, SR, diagram | Remove the arbitrary 100-note gate and distinguish adoption and review conditions based on actual utility, source-of-truth, external transmission, and operating costs. |
| 2026-07-25 | Next session task handover and phase boundary matching modifications | Phase 2b-A~E, Phase 2c·3, Physical AI image, AGENTS Korean view, `knowledge-graph` | Allows new sessions to implement immediately afterward without guessing installation location information or external transport boundaries, and matches any remaining basic-memory 100-note rules with the current decision. |
| 2026-07-25 | `notebooklm-export` skill removal (ADR 004) | All skills, `study-install` NotebookLM mode·`bootstrap.sh --notebooklm`, REGISTRY field, AGENTS·CLAUDE·README·Korean guide·MOC·Specs | 763 lines were maintained, but only 1 packet was actually used and 0 product recoveries. Manifests and hashes are ledgers for ‘returning later’, and without the act of looking back, they do not guarantee anything. CLI bridge is already blocked at the release gate. The ‘AI output is not independent evidence’ rule is maintained as it is also valid for direct browser use. |
| 2026-07-25 | Phase 2c basic-memory pilot implemented/not adopted | 2c Status Review Document, MOC | Run with custom corpus (`vault/공부`). Judging from a scratch copy rather than the original had split results — the index rewrote all 30 Markdown frontmatters, revealing that it was structurally unable to meet the read-only premise. Since the search quality and local embedding are good, it is not rejected, but is judged to be 'valid for legacy search if the user accepts the ownership price' |
| 2026-07-25 | Phase 2c premise measurements and pilot harness | `pilot.py`·Test, `knowledge-graph` skill, 2c status review document, MOC·Korean guide | After receiving the request to proceed, I checked the premise and found that PaperQA2 could not be executed because it had neither a provider nor a corpus, and for basic-memory, the corpus to be compared was determined by the user. Instead of inventing a procedure for a tool that cannot be run, first implement a safety device that is effective regardless of the corpus (disqualification and pre-blocking of write commands by comparing hashes before and after execution) |
| 2026-07-25 | Phase 3 — Diagrams, Learning Dialogue, Gardening, Review | `diagram`·`study-session`·`vault-gardening`·`recall` skills and 3 types of inspection scripts, 3 types of tests, AGENTS·README·Korean guidance·MOC | Verbal rules are not followed, so what can be determined is moved into scripts — diagram promotions are counted by node count, vault drift is reported in terms of contract violations, and review cards only pass when anchors are interpreted in citation notes. The warning that derivative works do not constitute evidence is common to all four. |
| 2026-07-25 | Phase 2b-E — NotebookLM release gate and pre-upload packet verification | `bridge-gate.sh`·`verify-packet.sh`·`bridge_test.sh`, `notebooklm-export` skills·`export.sh` slug verification, README·Korean guide·MOC·Specs | As a result of the re-audit, the code confirmed that there was no redirect modification in the latest stable release v0.7.3 (guard module 404), so installation, authentication, and transfer were not opened. Instead, it implements a gate that decisively reproduces the decision and a packet re-verification that is immediately useful for manual upload. |
| 2026-07-25 | Phase 2b-D — Obsidian Format/CLI Skills and Format Verifier | `obsidian` SKILL·`references/modes.md`·`scripts/validate.py`·2 types of tests, README·Korean instructions·MOC·Specs | Obsidian's own syntax appears correct when written from memory, but silently fails to render. All three types can be verified without an app, so we have a verifier, and the callout list is read from the pinned reference and synchronized upstream. It is stated that `.base` is a preliminary structural check as YAML verification is not possible due to the principle of non-dependency. |
| 2026-07-25 | `understand` 9 skills integrated into single skill internal routing | `understand` SKILL·`references/modes.md`·`routing_test.py`, 8 removed skill directories, AGENTS·CLAUDE·README·Korean information | User decision. Specification R2 requires the provision of 9 entry points, not 9 skills, and if the boundaries are repeated nine times, they will be out of sync. Routing table is placed in SKILL and mode-specific procedures are placed in references, returning the number of skills from 20 to 12. |
| 2026-07-25 | Phase 2b-C — typed knowledge graph and evidence verification inference layer | `build_graph.py`, `knowledge-graph` skills, `understand-knowledge`, AGENTS, README·Korean guide·MOC·Specs | For a graph to be useful even in a knowledge root with only one note, topics and sources in addition to links had to be nodes. At the same time, the inference layer is separated into opt-ins to prevent model inferences from being mixed with facts, and evidence anchors are accepted only when interpreted in the citation note, and the note body is not included in the derivative work. |
| 2026-07-25 | Phase 2b-B — 9 types of Understand Anything adapters | `understand`·`understand-chat`·`-dashboard`·`-diff`·`-domain`·`-explain`·`-figma`·`-knowledge`·`-onboard`, common `adapter-contract.md`, adapter matching test, AGENTS·CLAUDE·README·Korean Guide·MOC·Specs | To expose the upstream skill within the boundaries of this harness (graph is not evidence, output isolation, approval by execution) rather than opening it as is. Our investigation shows that the runtime is split into three layers within one pin, so the batch does not execute anything, so we move the gate from the installation point to the execution point — this opens up `understand-knowledge`, which runs without dependencies. |
| 2026-07-25 | Phase 2b-A — External source exact-pin installed base | `.tools/PINS.md`, `install-phase2b-tools.sh`, `tree_hash.py`, `study-install`, `.gitignore`, README·Korean guide·MOC·REGISTRY | In order to reproducibly place only the correct commit without an upstream global installer, the identity is fixed by tree contents rather than archive bytes, and all hash·tag·runtime·pin formats are blocked as fail-closed. |
| 2026-07-25 | Independent review intellectual reflection - hook stop contract, link analysis, installation observation, skill boundary | `study-wrapup`, `build_graph`, `web-capture`, `bootstrap`, `install-phase2-tools`, `defuddle`·`context7`·`paper-search`·`notebooklm-export`·`knowledge-graph`·`ingest`·`metaskill` Skills, AGENTS | The stop hook blocks paths that were infinitely blocked in forced continuation, matches file name/NFC link interpretation and installation success observations to actual operations, and restores fallback, border, and injection defenses missing during the porting process. |
| 2026-07-25 | Refactored root README and added mobile Telegram study bridge | root README, docs MOC, `docs/guides/mobile-telegram-bot.md` | We improved the excessive exposure of technical details in the root README and organized them around overview, quick start, and skill map, and officially added an asynchronous bot bridge (`_workspace/telegram_bot/`) that connects Mac harness and phone telegram based on a safe whitelist and a user guide. |
